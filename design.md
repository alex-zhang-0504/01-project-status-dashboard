# design.md — TDT 看板样式设计规范

> **改动前先读本文件。** 任何视觉改动都应落在本文件已定的框架内;框架内的调整直接做,框架外的先改本文件再改代码。
> 唯一正式视觉实现是 `src/template-dashboard-v1.3.0.html`（单文件、无第三方依赖、无前端构建链）。参照系是 macOS Numbers 的表格与图表样式；正式数据抓取、热更新和离线导出仍由既有 Python／lark-cli 链路负责。

## 0. 使用与维护约定

1. **先读后改**:改模板前先读本文件,确认要动的东西是否已有规定。
2. **规范先行**:实现时发现某个参数本文件没写、必须临时决定,那是规范的缺口——先补进本文件,再写代码,不在代码里就地决定。否则风格会散落到各处实现里,等于没有规范。
3. **冲突时以实测为准**:本文件与模板不一致时,以模板实测值为准,并回头更新本文件。
4. **状态标注**:**已实现** = 模板中已落地并在预览里核过;**规范已定,未落地** = 只有规范,模板中尚无对应代码。
5. **框架定位**:模板是未来看板的框架基座,不只服务当前这一个看板。所以本文件覆盖当前看板尚未使用的视图类型(饼图、折线图),新看板需要时直接取用。
6. **验证方式**：视觉模板改完后先运行正式项目的静态回归：`node tests/test_empty_table_cells.mjs`、`node tests/test_template_refresh.mjs`、`node tests/test_template_design.mjs`，再运行 `D:\ai-workspace\shared\scripts\python.cmd tests\test_dashboard_runtime.py`。需要核对真实 Base 数据时，使用 `start-dashboard.cmd --no-open --port <未占用端口>` 启动正式链路；模板 HTML／CSS／JavaScript 修改后刷新浏览器页面，生成器或启动逻辑修改后才重启服务。

## 0.2 Windows／macOS 兼容边界

- 视觉模板保持单文件、无外部字体和前端依赖。Windows 优先使用 Aptos／微软雅黑，macOS 依次回落到系统字体／苹方；不以 `@font-face` 内嵌商业字体。
- 自包含离线 HTML 可以在 Windows 与 macOS 浏览器直接打开。正式 Base 抓取依赖各设备本机已登录的 `lark-cli` 与启动配置；本项目的正式双击入口以 Windows `.cmd` 为基准，macOS 需使用独立的 `.command` 入口或自行提供等价本机配置。
- 所有视觉变更至少在 Windows 正式服务页核验；需要跨设备分发时，再用 macOS 浏览器检查字体回落和横向表格滚动。

## 0.1 不可逾越的边界

- 不改字段含义、统计规则、PM / 赛道排序、空值映射、风险映射等业务逻辑。
- 看板内容忠实映射用户在 Base 多维总表的输入:未填则空,填了 `-`、`/` 等任何字符就原样显示,渲染层不做替换、不补占位——规则因此简单统一,展示质量靠书写规范在源头保障。
- 不引入前端构建链、第三方依赖、外部网络请求或凭据；既有本地 `/api/data` 刷新请求属于正式运行链路，必须保留其当前错误处理与重试规则。
- 红黄绿是风险语义色,任何情况下不得挪作分类色。
- 会议纪律页从 `DATA.meetingDiscipline` 读取内容，但只允许改变内容来源，不得借数据化调整视觉。其排版独立于另外三个表格视图，不套用表格 `16px` 字号规则。

---

## 1. 色彩 Token（已实现）

全部定义在模板 `:root`。**同一用途只有一个来源,其余是别名。**

### 1.1 灰阶体系

统一为一套中性灰,不再有暖 / 中性两套并存。

| 层级 | Token | 值 | 用途 |
|---|---|---|---|
| 底 | `--bg` / `--surface` | `#fff` | 页面底、卡片底、表格底 |
| 弱底 | `--surface-2` | `#f6f6f6` | 表格隔行、`.badge.none`、`.issue-status.other`、禁用页签 |
| 弱底别名 | `--table-zebra` | `var(--surface-2)` | 表格隔行 |
| 线 | `--line` | `#e2e2e2` | **全站唯一线色**。卡片边框、分隔线、表格 hairline、图表网格线 |
| 线别名 | `--table-line` / `--chart-grid` | `var(--line)` | 表格 / 图表 |
| 加重线 | `--table-rule` | `#b8b8b8` | 首列深分隔线、合计行上边线、饼图引线 |
| 次要文字 | `--muted` | `#787878` | 刻度标签、辅助说明 |
| 正文 | `--ink` | `#363636` | 正文墨色 |
| 图表基线 | `--chart-axis` | `var(--ink)` | x 轴基线,别名 |
| 阴影 | `--shadow` | `0 1px 2px rgba(15,15,15,.04), 0 2px 8px rgba(15,15,15,.04)` | 只给卡片容器 |

- **全套灰阶均为纯中性(R = G = B)**,原 Notion 基线的暖调值(`#37352f`、`#787774`、`#e3e2de`、`#f7f7f5`)已全部替换,模板中不再出现。
- `--line-soft: #efefef` 当前全文件无引用,属预先存在的死 token,保留未删。

### 1.2 主题色

| Token | 值 | 用途 |
|---|---|---|
| `--accent` | `#0a9bf5` | 主强调色(Numbers 蓝)。页签、按钮、柱图、文字中的动态数字 |
| `--accent-soft` | `#e6f4fe` | 蓝色浅底(当前用于 `.pm-name.current`) |
| `--accent-ink` | `#fff` | 蓝底上的文字 |
| `--head-cell-bg` | `var(--accent)` | 表头底色 |
| `--head-cell-ink` | `var(--accent-ink)` | 表头文字 |
| `--head-cell-line` | `rgba(255,255,255,.34)` | 表头内部分隔线 |

### 1.3 风险语义色

| Token | 值 | 底色 |
|---|---|---|
| `--risk-hi` | `#d44c47` | `--risk-hi-bg` `#fdebec` |
| `--risk-mid` | `#a87608` | `--risk-mid-bg` `#fbf3db` |
| `--risk-lo` | `#448361` | `--risk-lo-bg` `#edf3ee` |

**红黄绿只属于风险语义。** 这条约束直接决定了饼图 / 多系列折线图不能用 Numbers 原生的彩色调色板(见 5.1)。

### 1.4 分类色板（规范已定，未落地）

| Token | 值 | 顺序 |
|---|---|---|
| `--chart-cat-1` | `#0a9bf5` | 1 |
| `--chart-cat-2` | `#4fb5f8` | 2 |
| `--chart-cat-3` | `#8ad0fb` | 3 |
| `--chart-cat-4` | `#c2e5fd` | 4 |
| `--chart-cat-5` | `#8e8e8e` | 5(其它 / 兜底) |

蓝色明度阶梯 + 中性灰。饼图与多系列折线图共用，**次系列一律取本色板，不另设浅蓝 token**（原 `--accent-hi` 已随柱图去渐变一并删除）。类别超过 5 个时**不扩色板**，改用柱图——再往下加，相邻两级在投影仪上分不开。

当前正式模板尚未使用饼图或多系列折线图，因此不提前写入未使用的运行时 token；归档样例 `docs/samples/pie-chart.html` 已使用本节同值。实际落地相应图表时，先将这五个 token 加入模板 `:root`，再复用，不得另设近似色值。

---

## 2. 字体与字号（已实现）

- `--dashboard-font-family`:`"Aptos", "Segoe UI", -apple-system, BlinkMacSystemFont, "PingFang SC", "Microsoft YaHei", "微软雅黑", "Hiragino Sans GB", sans-serif`
- 逐字形回落,实际结果:

  | 环境 | 英文 / 数字 | 中文 |
  |---|---|---|
  | Windows + Office 2024/M365 | Aptos | 微软雅黑 |
  | Windows 无 Aptos | Segoe UI | 微软雅黑 |
  | macOS | SF Pro | 苹方 |

- 不用 `@font-face` 内嵌:Aptos / 雅黑均微软版权,涉授权,且违反单文件无外部资产的边界。对外分发要求字体一致时,导出 PDF / 图片。

字号阶梯:

| 用途 | 值 |
|---|---|
| 表格正文 | `--table-font-size: 16px` / `--table-body-weight: 400` |
| 表头 | `16px` / `--table-head-weight: 700` |
| 会议纪律标题 `.meeting-title` | 24pt / 700 / 继承 `--ink` |
| 会议纪律正文 `.meeting-item` 及其子元素 | 15pt / 400 / 继承 `--ink` |
| 会议纪律金额 `.meeting-part.danger` | 15pt / 700 / `--risk-hi` |
| 会议纪律联系人 `.meeting-part.accent` | 15pt / 700 / `--accent` |
| 页标题 `.project-overview-title` | 24pt / 700 |
| 卡片概述 `.project-overview-summary` | 19px / 700,其中 `<strong>` 数字 24px |
| 详细进展 PM 摘要 `.pm-summary`、视图级空态 `.board-empty` | `16px` / 400 |
| 图表数值标签 | 15px / 600 / `--ink` |
| 图表分类标签 | 14px / `--muted` |
| 图表刻度标签 | 13px / `--muted` / `tabular-nums` |
| 右下角版权标识 `.copyright-mark` | 12px / `--muted` / opacity 0.62 |

表格字号由 token 统一控制,并以 `!important` 覆盖单元格内所有子元素,所以 `.code`、徽标等不会跑偏。`#board` 只提供 `13px / 400` 的根级兜底，禁止用 `#board *` 向全部子元素强制写入字号或字重；标题、概述及后续新增的内联文字必须正常继承其所在组件的字体层级。

会议纪律是独立的会议内容卡片，不属于表格。数据驱动渲染必须继续输出 `.meeting-title`、`.meeting-item`、`.meeting-marker`、`.meeting-part.danger` 和 `.meeting-part.accent` 结构，确保标题、正文、金额红色和联系人蓝色与解耦前完全一致。

- 项目概况内所有「YYYY年」由生成数据 `DATA.dashboardYear` 提供，取生成时的日历年份；不得使用 ISO 周所属年份或浏览器当前日期替代。年份统一包裹为 `.project-overview-year`，颜色使用 `var(--accent)`，字号和字重均继承所在标题或概述文字。

---

## 3. 表格（已实现）

覆盖详细进展 `.task-grid`、遗留问题 `.issue-table`、创新 `.innovation-table`、开发 `.development-table`。

- 详细进展 `.task-grid` 是连续的 CSS grid 表格，不是卡片式表格；沿用本节的单线网格、隔行底色与首列深分隔线。
- **表头**:实心 `--head-cell-bg` + 白字 + 700,内部分隔线 `--head-cell-line`,**无下划线、无外描边**
- **线条**:统一 1px `--table-line`
  - `<table>` 用 `border-collapse: collapse`
  - CSS grid 必须**手动模拟 collapse**:单元格只留 `border-right` + `border-bottom`,行首补 `border-left`。否则相邻单元格边框相接呈 2px,比 `<table>` 粗一倍
- **隔行**:偶数数据行 `--table-zebra`
  - `<table>` 用 `tbody tr:nth-child(even)`
  - `.task-grid` 每行是独立 grid 元素,用 `[role="table"] > .task-grid:nth-of-type(odd)`
- **首列**:右侧 1px `--table-rule` 深分隔线
- **合计行**:`border-top: 1px solid var(--table-rule)`,字重回 400
- **无圆角、无阴影**。阴影只给卡片容器

---

## 4. 图表通用规范（已实现）

两张柱图共用,新增图表沿用同一套坐标层。

- **轴层 `.chart-axis`**:绝对定位覆盖绘图区,`z-index: 0`,`pointer-events: none`;柱 / 线在 `z-index: 1`
- **水平网格线**:1px `--chart-grid`,只有横线,**无竖网格线**
- **基线**:轴层第一条(值 = 0)加粗为 `--chart-axis`。柱图 1.5px,折线图 2px
- **y 轴刻度**:左侧 46px 槽位,标签右对齐、`padding-right: 9px`、13px `--muted`、`tabular-nums`;**无 y 轴竖线**
- **刻度算法 `chartScale(max)`**：步长取 `[1, 2, 2.5, 5, 10] × 10ⁿ` 中首个 ≥ `max / 4` 的值，顶值 = 步长向上取整。当前两张柱图是计数图，步长最小固定为 `1`，不得显示 `0.25`、`0.5` 等分数任务刻度。柱高 / 点高的分母用 `scale.top`，不是数据 max
  - 与 Numbers 原生不同:Numbers 把数据 max 四等分再对标签取整(会出现 0/6/11/17/22)。此处选整步长,商务读数更利落
- **y 轴一律从 0 起**。非 0 起点会放大视觉波动,汇报场景属误导

### 4.1 柱状图

- 纯色 `--accent`,**直角**,无渐变、无阴影
- 数值标签在柱顶外侧,15px / 600 / `--ink`
- **数值标签需要柱顶之上约 22px(15px 文字 + 7px 间距)的净空**,布局必须给出这段余量,否则 `max` 恰为刻度步长整数倍(柱高 100%)时标签会顶出绘图区
- 尺寸(实测):

  | 图 | 容器 | 绘图带 | 标签带 | 净空来源 |
  |---|---|---|---|---|
  | 赛道图 | 390px | 226px | 90px | 绘图带上方 44px 余量(`.overview-bar-visual` 270 − 226) |
  | 产品代图 | 340px | 294px | 46px | 绘图带本身**无余量**,靠 `.product-generation-chart-scroll` 的 28px 上内边距容纳 |

  产品代图这条是既有实现的遗留差异,不是设计意图。新图一律按赛道图的做法在绘图带内部留余量,不要依赖外层内边距——外层 padding 一旦被调小,标签就会被裁。

---

## 5. 饼图（规范已定，未落地）

参考实现:[`docs/samples/pie-chart.html`](docs/samples/pie-chart.html)。单文件无依赖,双击可看;`renderPie(el, data)` 传类别数组出图,改文件顶部 `DATA` 换数据。

**适用场景窄**:仅用于部分-整体关系,类别数 ≤ 5。类别更多、或极值悬殊(最大项 / 最小项 > 20 倍)时改用柱图或列表,否则小扇区退化成不可读的细线。

### 5.1 配色

用 1.4 的分类色板。Numbers 原生五色是 蓝 `#0A9BF5` / 绿 `#5FD435` / 灰 `#8E8E8E` / 黄 `#F5B700` / 红 `#FF2600`,**其中绿、黄、红不采用**——它们已是风险语义色,照搬会让读者把红色扇区误读为高风险;蓝与灰保留(即 `--chart-cat-1` 与 `--chart-cat-5`),空出的三档改用蓝色明度阶梯填充。

### 5.2 形状

- 扇区之间**无描边、无间隙**;无阴影、无 3D、无圆角
- 起始角 12 点方向,顺时针
- 按数值降序排列,`--chart-cat-5` 固定给"其它"类
- 直径 260–320px
- 扇区内**不放任何文字**

### 5.3 数据标签与引线

**所有扇区一律用外部引线标签**,不做"大扇区放内、小扇区放外"的分支。理由:按阈值分流会让同一张图出现两种标签形态,读者要切换两次视线规则;且阈值附近的数据轻微变动会让形态跳变。

引线为两段式折线,无箭头、无圆点端子:

1. **径向段**:从扇区中心角方向、半径 r 处出发,沿同一角度外延 18px
2. **水平段**:从径向段末端延伸至同侧固定标签列 `x = cx ± (r + 18 + 20)`。因终点是固定列,该段长度随扇区角度变化,3 点 / 9 点方向最短 20px,越靠近 12 点 / 6 点越长;同侧标签因此对齐成一列
3. 标签紧接水平段末端,留 8px 间距;左侧标签右对齐,右侧标签左对齐

样式:

- 引线 1px `var(--table-rule)`,**统一中性灰,不跟随扇区色**。多色引线让图变花,且引线不承载分类信息
- 标签 16px / 400 / `var(--ink)`,内容为 `类别名 45%`,中间空一格
- 百分比保留整数,四舍五入后总和不为 100% 时不强行凑整
- 文字不压在扇区上,所以**不需要按底色切换标签颜色**,统一 `--ink`

防重叠:

- 同侧标签按 y 排序,相邻两条间距不足 22px 时依次向下推挤
- 推挤后**必须双向回夹**:先查最后一条是否越过下边界(越过则整组上移),再查第一条是否越过上边界(越过则整组下移)。只做单向修正会让顶部标签被容器裁掉
- **回夹边界是容器边内缩半行文字(10px),不是留白值 `PAD_Y`**。用 `PAD_Y` 当边界会把本来没冲突的标签整组挪走,水平段随之全部变斜,同侧标签越多越明显
- 被推挤的那条,其水平段会呈轻微倾斜,属可接受形态,不额外加第三段强行拉平
- 类别数已限定 ≤ 5,正常不触发多轮推挤;若同侧超过 4 条标签,说明该数据不适合饼图

容器留白:

- 宽度 = 直径 + 左右各 160px(引线 38px + 标签最大宽约 120px)
- 高度 = 直径 + 上下各 34px(径向段 18 + 半行文字 10 + 余量 6)

### 5.4 图例

**默认省略**——外部引线标签已带类别名,再放图例等于同一信息出现两遍。仅当扇区多、引线标签被迫简化为纯百分比时才启用。

启用时:顶部横排居中,实心圆点直径 10px + 标签,圆点与标签间距 8px,条目间距 24px,标签 16px / 400 / `--ink`。

### 5.5 边界情况

- 单类别(100%):画完整圆,同样走外部引线标签,不在圆心放字
- 全部为 0 或无数据:不画圆,走 `.board-empty` 空态文案,不画灰色占位圆

---

## 6. 折线图（规范已定，未落地）

用于时间序列(周度 / 月度趋势)。

### 6.1 坐标层

复用第 4 节的 `.chart-axis`。差异一处:**基线加重到 2px `--chart-axis`**(柱图 1.5px),Numbers 折线图的基线明显重于柱图。

尺寸(与赛道图同一套比例,自上而下相加):

```
上内边距  8px
余量     44px   ← 最高点之上的净空
绘图带  226px   ← 刻度 0 到顶值
分类标签 90px
────────────
容器    390px（余下 22px 为底部松弛）
```

左侧刻度槽 46px。折线图无柱顶数值标签,44px 余量用于防止最高点顶边和线宽(5px)溢出。

坐标规则:

- **x 轴用 point 模式,不是 band 模式**:首点贴绘图区左边缘、末点贴右边缘,中间等距,第 i 点 x = `i / (n - 1) × 绘图区宽`。单点时居中
- 分类标签与数据点同 x、居中对齐,首末两个标签允许溢出容器边缘
- 空值(某期无数据)**断开线段**,不插值连接,也不画 0 值点

### 6.2 线形：两个变体都提供

共用参数(两变体完全一致,只有路径生成方式不同):

- 线宽 5px;`stroke-linejoin: round`、`stroke-linecap: round`
- **无数据点标记、无面积填充**
- 多系列取 `--chart-cat-1..n`,靠色相区分,不做透明度区分
- 实现上是同一个 `<path>`,只替换 `d` 的生成函数;以修饰类切换 `.chart-line` / `.chart-line--smooth`

**变体 A:直线折线(默认)**

- 相邻点直线连接,不插值。Numbers 默认即此形态
- 适用:离散周期快照(周度计数、状态数、人数),点与点之间没有真实中间值

**变体 B:平滑曲线**

- 用**单调保形三次插值**(monotone cubic,Fritsch–Carlson),**不用 Catmull-Rom**
  - Catmull-Rom 会在拐点 overshoot,画出高于实际最大值或低于 0 的弧。本看板的量都是非负计数,穿到基线以下就是读数错误
  - 单调保形插值保证曲线不越过相邻两点构成的值域
- 适用:强调趋势形状、数据点较密(≥ 8 个)、底层是连续量

**选用规则**:默认变体 A。只有数据点密到折线发锯齿、且底层确实是连续量时才换 B。同一张图不混用。

### 6.3 图例

顶部横排:短线段 18×4px、圆角 2px + 标签,标签 16px / 400 / `--ink`。条目超出容器宽度时换行,行距 8px;不做横向滚动、不省略条目。

---

## 7. 卡片、徽标、警告与空态

### 7.1 卡片

| 元素 | 规格 |
|---|---|
| `.project-overview-card` | 1px `--line`、圆角 8px、内边距 `26px 28px 18px`、白底、`--shadow` |
| `.overview-card`(TDT 名称条) | 1px `--line`、顶边 3px `--accent`、圆角 6px、内边距 `10px 14px`、`--shadow` |
| `.innovation-summary-panel` | 1px `--line`、圆角 7px、内边距 `22px 20px`、**白底**(不用弱底) |
| `.development-summary-panel` | 1px `--ink`、内边距 `20px 18px`、白底 |

阴影只出现在卡片层,表格和图表内部一律无阴影。

### 7.2 徽标

`.badge`(风险)与 `.issue-status`(问题状态)同规格:`inline-block`、圆角 999px、内边距 `3px 8px`、`white-space: nowrap`,配色走风险语义色的 `-bg` + 前景色;无风险 / 其它状态用 `--surface-2` + `--muted`。

### 7.3 空态

- 单元格级:`null`、空串、纯空白 → 渲染空白。用户明确输入的 `/` 原样显示为 `/`,不当作空值
- 视图级:无数据时用 `.board-empty`,`16px / 400`、`--muted`、居中、上下内边距 70px

### 7.4 数据质量警告（已实现）

- 本周新增子任务存在赛道空值时，在项目概况第一张卡片的概述下方、图表上方显示 `.weekly-addition-warning`；它是给看板操作者的数据质量提示，不属于汇报句，也不伪装为 Base 字段内容。
- 文案明确说明错误数量及涉及的项目编码／名称，并提示补充赛道后重新生成；项目编码为空时只显示项目名称，不补造占位值。
- 容器使用 `1px var(--risk-mid)` 边框、`var(--risk-mid-bg)` 背景、6px 圆角、`10px 12px` 内边距，正文 `14px／400／1.6`；“数据质量提醒”使用 `var(--risk-mid)` 与 700 字重。黄色在此表达真实的数据质量风险，不用于图表分类。
- 无新增或本周新增记录的赛道均非空时不渲染该容器；全部新增记录均缺赛道时仍显示警告，但汇报句只显示“本周新增X个。”。

---

## 8. 套用规范时的输入与产出

**大原则**:有了数据之后,照本文件实现即应直接得到目标风格,不再重新做视觉决策。

实现一张新图需要提供的输入只有三项:

1. 数据数组(饼图:类别 + 数值;折线图:系列 + 每期数值)
2. 图表类型与变体(柱 / 饼 / 折线-直线 / 折线-平滑)
3. 容器宽度(高度由本文件给定,不需要传)

其余全部由规范定死:配色、刻度算法、网格线与基线、线宽与端点、标签字号与颜色、图例位置与换行、空数据与单类别处理。

---

## 9. 决策记录

记录格式:**改了什么 / 原状 / 为什么**。带"推翻"标记的条目说明该方向已被否决,不要重新提案。

### 2026-07-24

**字体栈改为跨平台栈**
原状:`"Microsoft YaHei"` 打头的 Windows-only 栈。
理由:该栈在 macOS 上找不到雅黑,直接掉到默认 sans-serif,字发虚。改为逐字形回落的跨平台栈后,英文数字走 Aptos、中文走雅黑 / 苹方,一条栈同时满足两个平台。

**表头去饱和** —— ⚠️ 已于 07-26 推翻
原状:4 处深蓝实心表头。当时改为中性灰底 + 深墨字 + 2px accent 下划线,理由是深蓝实心在长表格里视觉压迫。

### 2026-07-26

**主题色换 Numbers 蓝 `#0a9bf5`**
原状:`--accent: #1677d2`。
理由:整体参照 macOS Numbers。文字中的动态数字本来就绑 `var(--accent)`,换 token 即全局生效,不需要逐处改。

**表头改回实心蓝 + 白字** —— 推翻 07-24 的"表头去饱和"
理由:Numbers 的表格表头就是实心亮蓝 + 白字,与去饱和是相反方向,只能二选一。选了贴 Numbers。**不要再提去饱和方案。**

**表格 Numbers 化**
原状:1px `--line` 边框 + 表头 2px accent 下划线,`.issue-table` 带 `box-shadow`,无隔行底色。
理由:Numbers 表格的识别特征是 hairline + 隔行明暗 + 首列深分隔线 + 完全扁平。去掉阴影和圆角后,表格从"卡片"退回"表格",长表扫读更稳。

**柱图 Numbers 化 + 刻度算法**
原状:柱是 `linear-gradient` + 7/8px 圆角 + 阴影;无 y 轴、无网格线;柱高分母是数据 max。
理由:Numbers 柱图是纯色直角。分母从数据 max 改为 `scale.top`,否则最高柱永远顶格、看不出余量。刻度选整步长而非 Numbers 的"max 四等分",因为后者会产生 0/6/11/17/22 这种刻度,商务汇报读数不利落。

**创新大赛并入项目价值概况**
原状:独立 `<h2>创新大赛项目概况</h2>` + 卡片。
理由:该节内容量小,独立成节与前后两节的权重不匹配;并入后成为价值概况的第三张卡。`aria-labelledby` 相应改为 `aria-label`,不丢无障碍名。

**表格字号统一 16px**
原状:`--table-font-size: 17px`,字重已经是表头 700 / 正文 400。
理由:统一到 16 px 的整数阶梯。字号由 token 一处控制并 `!important` 覆盖单元格内所有子元素,所以 `.code`、徽标不会跑偏。

**饼图分类色板用蓝阶梯 + 中性灰**
备选:Numbers 原生五色(蓝绿灰黄红)。
理由:绿 / 黄 / 红已是本看板的风险语义色,照搬会让读者把红色扇区误读为高风险。

**折线图两个变体都提供**
理由:直线折线贴 Numbers 原生形态、适合离散周期快照;平滑曲线适合连续量。模板是未来看板的框架基座,两种数据形态都会遇到,所以都定进规范。平滑用单调保形三次插值而非 Catmull-Rom,后者会 overshoot 到基线以下,对非负计数属读数错误。

**本看板暂不在 Claude Design 中设计**
理由:Claude Design 消费组件库、不生产组件库,`/design-sync` 的前提是仓库中已有组件库。本项目是单文件 HTML,无组件库,不满足前提。前置条件是先建立 design system。

### 2026-07-27

**饼图数据标签全部改为外部引线**
原状:规范原本写"标签放扇区内,占比 < 5% 时移出"。
理由:按阈值分流会让同一张图出现两种标签形态,读者要切换两次视线规则;且阈值附近的数据轻微变动会让形态跳变。统一外置后,"按扇区底色切换标签颜色"的规则连带作废,图例也因信息重复而默认省略。

**详细进展网格线改单线**
原状:`.head-cell` / `.data-cell` 四边都是 1px,`gap: 0` 时相邻边框相接呈 2px,比 `border-collapse: collapse` 的 `<table>` 粗一倍。
理由:CSS grid 没有 `border-collapse`,必须手动模拟——单元格只留右 / 下边框,行首补左边框。

**创新汇总面板去灰底**
原状:`.innovation-summary-panel` 用 `var(--surface-2)` 浅灰底。
理由:与相邻表格的白底不一致;改为白底 + 1px 细边框,靠边框而非底色划分区域。

**灰阶统一为一套中性灰**
原状:暖调(Notion 基线遗留)与中性两套并存 —— `--line #e3e2de` / `--table-line #e2e2e2`、`--surface-2 #f7f7f5` / `--table-zebra #f6f6f6`、`--ink #37352f`、`--muted #787774`。
理由:同一用途存在两个近似值,改配色要改多处且容易漏。收敛为 `--line` / `--surface-2` / `--ink` 三个来源,其余全是别名,全套 R = G = B。

**规范合并为单一文件 `design.md`**
原状:`docs/design-system.md`。
理由:规范只能有一份,否则会分叉。放项目根目录并在 `AGENTS.md`、`README.md` 中要求视觉改动前先读，提高被读到的概率。

**删除 `--accent-hi`**
原状:07-24 为柱图渐变引入的浅蓝 `#6ec4f8`,07-26 去渐变后引用数归零。
理由:死 token。次系列浅蓝的角色由分类色板的 `--chart-cat-2` 承担,两者本就重复。

**正式视觉基准确认**
确认项:表格表头／正文统一为 `16px／700／400`；项目概况两张计数柱图均恢复纵轴刻度和横向网格；低计数图的 `chartScale()` 最小步长固定为 `1`；会议纪律继续保留正式文案，不以视觉包中的示例文字替换。上述规则已同步进正式模板、项目约束、README 与自动化回归。

## 10. 待确认

- [ ] Numbers 蓝表头 + 隔行底色在打印 / 导出 PDF 下的表现,未验证。
- 数据来源与落点:**明确搁置,不作为待办**。饼图 / 折线图用什么数据、放哪一页,留到真有需要该视图的看板时再定。
