# ROADMAP

## 当前阶段

阶段 3：妙搭全栈迁移完成并切换正式 Base（本地 `V1.3.0` 正式入口已通过真实 Base、浏览器重新生成和离线产物验收；妙搭 App commit `ff1def1`、release `7673379105845677006` 已发布完成，开发预览态与线上发布态均通过创建者真实数据冒烟验收）。

## 已完成

- 2026-08-13：修复妙搭发布态与开发预览态不同步：确认远端 `sprint/default` 和既有 release 均为 `ff1def1` 后，在妙搭开发页执行“拉取”，平台提示“代码已同步”，内嵌预览由 `V1.2.0` 切换为 `V1.3.0`。开发预览真实生成 2026 W33、102 个详细进展子任务；项目概况显示总数 690、本周新增 1 个、其中通信 1 个，验证本次新增汇报规则实际生效。妙搭拉取任务自动发现并提交平台生成的 `server/database/schema.ts` 变更和 Hook 可执行位，形成仅在云端工作区的 checkpoint `a01574e`、`b27e66c`；因涉及未授权的数据库 schema 附带改动，两笔均未 push、未纳入发布。改由明确部署远端 `ff1def1` 的 release `7673379105845677006` 完成发布，`release-get` 返回 `finished` 且无错误；线上真实生成同一 W33、102 个子任务，最近 15 分钟 ERROR／WARN 日志为空。用户全局 `AGENTS.md` 已新增妙搭发布态／开发预览态分层验收及自动 checkpoint 防混入规则。
- 2026-08-13：按 SemVer 将正式看板升级为 `V1.3.0`，在项目概况第一张柱状图概述后按 Base 系统 `created_time` 和生成日期所在 ISO 周统计本周新增：有新增时追加总数与非空赛道分布且不重复“赛道”二字，无新增时不追加；赛道空值仍计入新增总数，但仅在独立数据质量警告中列出数量及项目编码／名称，系统创建时间缺失或无法解析时不猜测并警告。本地生成链路、正式模板、Windows／macOS 入口、规范、README 和回归测试已同步；妙搭服务端显式请求 `automatic_fields=true`，在线页与离线模板使用同构契约，形成 App 功能提交 `cab8bbb`。本地完成 30 项 Python 回归、4 项 Node 模板回归与 Python 编译；App 完成 23 项 Jest、lint、类型检查、生产构建和五项部署产物门禁。妙搭本地双层运行以真实 Base 完成首页、首次生成、项目概况、重新生成及离线 HTML 落盘验收；当周真实新增为 0，页面未追加文案且无赛道缺失误警告。lark-cli 用户身份增量授予 `base:record:retrieve` 后，以全新 `cmd.exe` 运行正式 `start-dashboard.cmd`，8710 服务安全替换为 `V1.3.0` 并自动导航到带新 `build_id` 的页面；页面内重新生成成功，生成时间由 03:08 更新到 03:09，当天离线 HTML 同步覆盖且内嵌本周新增、赛道空值和无效创建时间三项零值。本地入口完成。
- 2026-08-13：对用户确认属于中止误操作的妙搭远端提交 `1592717`、`05bd9c9`、`b529bdc` 逐项评估后，确认其脚手架降级、锁文件改写和占位页面生成均非正式版本所需；保留提交历史并由 `ff1def1` 撤销其净变更，最终相对上一发布基线只保留 `V1.3.0` 需求相关文件。`sprint/default` 已 fast-forward push 到 `ff1def1`，release `7673356066290093028` 返回 `finished` 且无发布错误。创建者线上页面完成首页、`GET /api/data` 首次生成、项目概况、重新生成和离线下载验收；刷新时间由 11:43 更新到 11:44，下载文件为 `V1.3.0` 自包含 HTML、无外链脚本或样式，最近 15 分钟 ERROR／WARN 日志均为空。
- 2026-08-02：用户分别确认妙搭开发页和线上发布页均可正常抓取正式 Base、生成看板并下载离线版；发布后近两小时线上 ERROR／WARN 日志均为空，请求与延迟指标接口未返回采样点，因此只记录“未观测到日志异常”，不虚构性能结论。新增根目录 `MIAODA_LOCAL_TO_ONLINE_RULES.md`，把本地全栈选型、双仓库隔离、环境变量生效、Base 权限、官方脚手架对齐、部署产物契约、Git／云端 AI 并发冲突、release 与线上冒烟门禁、平台故障分层及归档清单固化为跨 Project 可复用规则；App README 同步正式 Base 与高级权限要求并形成独立本地提交 `4da4c7d`。
- 2026-08-02：将本地妙搭配置、macOS 备用启动入口以及妙搭开发／线上环境的进展、遗留问题和创新大赛数据源统一切换到正式 Base；Windows 正式入口原已使用相同正式数据源，无需修改。正式 Base 将既有 CLI 文档应用调整为「可管理」后，应用身份对三个目标视图和记录抽样读取均返回成功；使用当前 App 服务端代码完成全量分页与业务装配，得到 15 个 PM 分组、15 个赛道、9 个创新大赛项目、2 个问题周次组、2 个会议区块和 11 条既有数据格式警告。三个妙搭环境的 URL 变量逐项回读一致；针对环境变量变更重新发布 App 提交 `1819796`，release `7669229089128172732` 返回 `finished`。创建者线上会话完成首次生成、再次重新生成和离线下载验证，正式数据源切换完成。
- 2026-08-01：完成妙搭开发沙箱 Git 冲突的本地根因修复并形成 App 提交 `1819796`：使用官方 `@lark-apaas/fullstack-cli 1.1.58` 模板同步 11 个平台脚手架文件，合并新版构建、依赖裁剪、开发保活和四步本地启动逻辑；保留 Windows 下通过 Node 加载 npm CLI 的 lint 兼容方式，并把 `upgrade` 改为直接执行 CLI 编译入口，规避官方 bin 将 `D:\` 路径误作 ESM URL 的问题。`package-lock.json` 已按最终 `package.json` 重新生成，未采用妙搭临时任务中删除 lockfile 的方案；提交已 fast-forward push 到 `origin/sprint/default`，远端完整 SHA 为 `18197962bea154305cb07157097a4a08cbe9f43f`。妙搭开发沙箱已中止未完成合并并 hard reset 到该远端提交，全部冲突文件和冲突标记均已清除；发布态不受影响。
- 2026-08-01：修复妙搭部署产物与运行时契约不一致：把 `run.sh`、精简 `package.json` 和生产 `node_modules` 调整到 `dist/` 根目录，服务端入口保持 `dist/server/main.js`，客户端 HTML 调整到 `dist/dist/client/index.html`；启动脚本改为从部署根目录执行 `NODE_ENV=production node server/main.js`，使用 LF 并保留 Git 可执行位。新增发布产物回归和统一 `npm run verify:release` 门禁，父项目与 App 规则均明确区分应用产物缺陷和 TrustLayer／内部 DNS／网关／FaaS 平台故障。完整门禁通过，形成 App 提交 `8ca2c8e` 并推送到 `sprint/default`；release `7669059896394321192` 返回 `finished`，妙搭服务端将发布态 `main` 推进为合并提交 `e8391da`，发布阶段不再出现 `run.sh` 缺失或内部 DNS 502。创建者发布态验收确认首页、`GET /api/data`、重新生成和离线下载均可用，妙搭迁移完成。
- 2026-08-01：按 SemVer 将正式看板升级为 `V1.2.0`：标准 ISO 周锚点和会议内容解耦属于向后兼容的业务能力变更，因此升级次版本；新增并启用 `src/template-dashboard-v1.2.0.html`，保留 `V1.1.0` 回退模板，同步 Python 默认参数、HTTP Server 标识、Windows／macOS 四个入口、项目规范、视觉规范、README、迁移方案和测试目标。妙搭在线页与离线模板同步为 `V1.2.0`，功能提交为 `9f63ecb`；通过 `01f10dc` 接入妙搭远端初始化历史并同步到 `sprint/default`，尚未发布。
- 2026-08-01：完成遗留问题周次锚点与会议内容解耦并形成 App 本地提交 `38dc274`：本地 `V1.1.0` 与妙搭版均以 `Asia/Shanghai` 生成日期所在 ISO 周为本周、日期减 7 天为上一周，只读取两个标准周次；当前 W31 验证确认 W29 不再进入看板。会议纪律改由入库内容源驱动，继续保留原 `24pt／15pt` 字号、风险红色和联系人蓝色，未套用三个业务表格的 `16px` 规则；妙搭首次读取失败时保持可重试空态，未知服务异常不再向客户端暴露堆栈。使用同一副本 Base 逐字段对账，除两种实现的诊断容器差异外业务数据差异为 0，均得到 15 个 PM 分组、98 个详细进展项目、664 条概况记录、9 条创新大赛记录、W30／W31 各 0 条问题及 2 个会议区块；本地 21 项 Python 回归、模板回归、妙搭 13 项 Jest、lint／类型检查、生产构建和正式 Windows 双击入口均通过。App 尚未 push 或发布，仍使用临时 CLI 应用身份。
- 2026-08-01：初始化独立妙搭 App 仓库 `deployments/miaoda-app/`，确认开发分支为 `sprint/default`、远端由妙搭管理，父 Git 继续忽略 App 目录；完成 Windows 构建／Git hook 兼容修复并形成 App 本地初始提交 `bca2481`，尚未 push、发布或扩大应用权限。
- 2026-08-01：完成妙搭版第一阶段代码迁移并形成 App 本地提交 `bb45d60`：建立 `V1.1.0` 前后端共享数据契约，将父项目的层级识别、例会筛选、进展解析、日期、PM 分组、项目概况、开发达成、遗留问题和创新大赛规则迁移为纯 TypeScript；正式单文件页面改接 `GET /api/data`，保留刷新重试和失败时上次成功数据，并新增按需下载自包含 HTML。插件未配置时接口明确返回 `503`，不以空数据伪装成功。
- 2026-08-01：核对妙搭官方多维表格插件说明，确认插件以应用 Owner 身份操作、Owner 必须是 Base 管理员、单个实例只能绑定一张表；固定方案调整为三个只读插件实例分别绑定进展、问题和创新大赛数据表。因当前尚未取得妙搭实际生成的插件标识与调用定义，未猜测依赖名，也未改用需要新增凭据和权限的 OpenAPI 路线。
- 2026-08-01：因官方多维表格插件无法满足正式契约依赖的 Lookup 字段读取，迁移路线统一切换为 Base OpenAPI。以用户身份对三张副本 Base 完成只读预检：总表「阶段查找／状态查找」返回真实值，创新大赛和遗留问题视图均可分页读取；未修改任何 Base 数据。
- 2026-08-01：完成妙搭服务端 Base OpenAPI 数据源并形成 App 本地提交 `60a826b`：统一获取应用 `tenant_access_token`，校验三条 Base URL，读取视图／字段元数据并分页查询记录；补齐 Lookup、公式、富文本和关联记录的兼容转换，主表保留字段选项顺序。三张副本 URL 与 16 人 PM 相对顺序仅写入被 Git 忽略的 `.env.local`，凭据键名以 `.env.example` 固化，真实值不入库；尚未 push 或发布。
- 2026-08-01：既有 CLI 应用补齐 `base:record:retrieve` 后，以应用身份对同一副本 Base 中的三张数据表完成只读端到端验证：三次记录查询均返回 `HTTP 200／code 0`，妙搭本地代理 `/api/data` 返回 `HTTP 200`，得到 15 个 PM 分组、98 个详细进展项目、664 条概况记录、9 条创新大赛记录和 1 条遗留问题，Lookup 相关警告为 0。修复 Windows 下 HTML 内联模块生成无效代理路径的问题，前端入口返回 `HTTP 200`，形成 App 本地提交 `5853412`；尚未 push、发布或配置妙搭远端环境变量。
- 2026-07-31：启动妙搭全栈迁移并固化 `docs/miaoda-full-stack-migration-solution.md`，明确当前 Project 统一管理、`deployments/miaoda-app/` 独立 Git、父 Git 版本档案、仅创建者验证和本地 `V1.1.0` 回退边界；父 Git 忽略规则已通过 `git check-ignore` 验证。完成 `spark:app:write` 最小范围授权，创建妙搭全栈应用 `app_17b7awscgpe`，未添加协作者或其他操作者，尚未初始化本地 App 仓库。
- 2026-07-10：确认采用本地代码开发，项目路径为 `projects/01-project-status-dashboard/`。
- 2026-07-10：建立项目规范、说明、进度记录、忽略规则和源码目录边界。
- 2026-07-10：验证项目仅包含计划内文件，且不存在空目录、依赖、凭据或功能代码。
- 2026-07-11：设计方案与用户逐项确认：卡片流主视图（按 TDT PM 分组）＋风险统计/筛选＋PM 导航；卡片含里程碑线（启动→TDR1→KO→TDR3）、控制点折叠、三栏进展；双配色（Notion 纯白/Claude 暖色）可切换；视觉基准为会话中确认的 demo 预览页。
- 2026-07-11：确认数据规则：demo 白名单（3 个 TDT 下 4 个子任务）、字段映射、里程碑延期判定（无实际且计划早于当天→红）、技术迁移阶段暂用开发阶段控制点、风险「/」灰徽章不计入统计。
- 2026-07-11：完成 `src/template.html`（双配色/深浅色/筛选/导航/折叠，来自 Base 的文本统一转义）与 `src/generate_dashboard.py`（视图校验、分页读取、白名单「父 TDT record_id＋子任务名」精确匹配、里程碑状态计算、进展解析注入模板）。
- 2026-07-11：真实数据跑通并验证：4 个白名单子任务全部精确匹配、零警告；抽 B260026A/B260084B 两条记录逐字段核对一致（含 KO 逾期→延期红点、TDR1 显示实际日期、最新日期块选取与本周/下周拆分）；确认 `demo_scope.json` 与 `output/` 未进 git。
- 2026-07-11：按用户反馈迭代一轮：卡片头部增加所属 TDT 名（TDT · xxx）；全局字体改微软雅黑；卡片主体（里程碑/控制点/三栏）字号加一号；控制点取消折叠常开展示；上半区改为与三栏同轨的等宽分栏（里程碑占第 1 栏、控制点跨 2～3 栏），中线上下对齐；重新生成并验证产物。
- 2026-07-11：里程碑标签 KO 更正为 TDR2（数据层，两版式同步生效）；白名单扩充「功耗性能技术」TDT（PM 杨洪元）前三个子任务（B250152A/B/C），验证多 PM 分组样式。
- 2026-07-11：完成 demo2（方案 C 列表＋详情面板）：左栏 180px、TDT＞子任务父子层级、子任务可点选；右侧五模块（里程碑/控制点/本周进展/下周计划/风险描述）各占一行，高亮条＋高亮块样式（token 化，参考 CN6 报告）；风险筛选联动侧栏并自动切换选中项；7 个子任务全部精确匹配、零警告，产物结构检查通过。计划文档：docs/plans/2026-07-11-demo2-list-detail-panel.md。
- 2026-07-11：demo2 版式迭代两轮：侧栏按 PM 分卡片、字号统一 13px（TDT 粗/子任务常规）；改为按 PM 分节布局（左卡片与该 PM 汇报区等高、卡内列表吸顶跟随、每单元独立选中），侧栏加宽至 240px。
- 2026-07-11：收录规则新增字段筛选模式（--filter-field/--filter-value，勾选框 True 亦命中），白名单保留为默认；项目风险 空//、暂无、无 归为无风险灰色显示；风险描述模块色调与其他模块统一。切换新 Base（表 tblclCqOwpBgjxYS）按「是否上周三例会=是」生成 demo2：命中 18 个子任务、4 个 PM 分组，产物检查通过（无红色模块残留、暂无已归空）。
- 2026-07-11：分组键改为「子任务 PM」（缺失回退 TDT PM），demo2 详情标题 20px→18px；用户确认 demo2 定稿冻结。
- 2026-07-11：完成 demo3（人名滑动条版）：脚本数据层新增全表子任务 PM 名单（allPms，本次 74 人）；滑动条中有汇报项目的 3 人（周诚惠/杨洪元/张思远）按最左/中/最右分布可点选，其余置灰填充；同屏仅一个 PM 单元，点人名整体切换且各 PM 选中状态保留。真实数据生成验证通过（19 个子任务、3 个激活 PM）。
- 2026-07-11：demo3 迭代：滑动条改用 pm_roster 固定 16 人名单（存本地 demo_scope.json，不入库），详情标题字号 18px→15px；新增「AI 看板」按钮三态流程模拟 AI 平台集成——初始仅顶栏＋空态提示，点击后生成中动效 3 秒，随后筛选行/看板/页脚淡入揭示，按钮转「重新生成」可重播；样式确认后待同步 demo1/demo2。
- 2026-07-11：实现真实刷新：generate_dashboard.py 抽出 build_data()/load_scope_file() 供复用；新增 serve_dashboard.py 本地服务（127.0.0.1:8765，/ 出页、/api/data 实时重读总表）；demo3 模板改造为 hydrate() 数据装配＋http 模式下点击按钮 fetch 真数据（生成中动画持续到返回、失败回退旧数据提示），file:// 打开自动退回内嵌快照模式。实测 /api/data 刷新 7.2 秒、数据截至时间戳随刷新更新；静态生成回归通过。
- 2026-07-11：AI 按钮＋真实刷新同步到 demo1/demo2（同款 hydrate 改造与三态流程）；新增双击启动 start-dashboard.cmd（真实 URL 版 gitignore，example 版入库，默认 demo3，可传参换版式/端口）。验证：三版产物均含按钮/hydrate/SERVED 结构、模板 JS node --check 通过、cmd 启动链路实测 200 并正确出 demo3 页面。
- 2026-07-12：修复启动 cmd 两个问题：LF 行尾导致双击解析错位（转 CRLF，并以全新 cmd 进程模拟双击复测通过）；example 占位模板加占位符守卫，误双击输出复制填写指引。
- 2026-07-12：补齐架构学习文档 docs/architecture-learning-guide.md（链路逐环节解剖＋五层知识路线图＋动手练习）。
- 2026-07-12：README 增加「交接速览」——2026-07-14 起项目由 Codex 接管：三个运行入口、三个本地不入库文件、lark-cli 需非沙箱执行的注意项、决策背景文档索引。
- 2026-07-12：跨设备交接准备：新增 docs/session-handover-digest.md（设计偏好脉络/demo 决策状态/协作习惯/Artifact 链接/下一步）；新增 macOS 启动脚本 start-dashboard.command（真实版 gitignore＋example 版入库，LF 行尾核验 0 CR）；新增 .gitattributes 锁定脚本行尾（.cmd/.bat=CRLF，.command/.sh=LF），防 autocrlf 检出破坏。
- 2026-07-15：完成 demo4 HTML：顶部改为「概况/进展/问题」三级视图（仅进展启用），保留 PM 滑动轴并移除左侧栏；主内容按 PM 切换、按 TDT 合并展示，TDT 技术项目与价值说明采用同宽度边界双卡布局，同一 TDT 多个子任务共用一行九字段头。九字段比例锁定为 10/6/6/7/8/15/20/20/8；价值决策映射启动时间、结项时间映射 TDR3预计，统一显示 MM/DD（YY）。完成两轮 review 后真实生成 19 个子任务、3 个 PM 的 demo4 产物。
- 2026-07-16：按视觉反馈完成 demo4 字段排版调整：TDT 与价值说明双卡去掉小标题、直接显示正文；九字段内容统一为微软雅黑 13px（含项目编码、日期、控制点与风险徽章）；所有字段内容垂直居中，前五列同时水平居中。重新生成真实数据产物并完成需求复核与 Demo 1～3 回归。
- 2026-07-16：继续收敛 demo4 主内容字体与节奏：主内容全部锁定为微软雅黑 13px、常规字重，仅 TDT 技术项目名称加粗；价值决策与结项时间改为 YY/MM/DD；本周进展和下周计划采用 13px 行高，条目之间保留 13px 间隔。真实数据重新生成并验证通过。
- 2026-07-16：按真实风险内容调整 demo4 九字段比例为 10/6/6/6/7/14/20/20/11；风险列由 8% 扩至 11%，风险状态、项目阶段、控制点分别收窄至 6%、7%、14%，本周进展与下周计划继续各占 20%。重新生成最新 Base 数据产物并验证通过。
- 2026-07-16：按参考截图量化并调整 demo4 要点节奏：本周进展、下周计划、风险三列保持 13px 单条内部行高，相邻要点之间统一留 26px 垂直间隔，最后一项不追加底部空白。重新生成最新 Base 数据产物并验证通过。
- 2026-07-16：完成 demo4「问题」视图：保持顶部布局，点击「问题」后隐藏 PM 轴并在原位置显示周次与 OPEN/CLOSED 状态摘要；问题 Base 只读选择最新有效 Week，`Week` 不进入表格；正文采用十一字段连续表格、共享边框与蓝色字段头，按参考表比例设置 3.4/16.3/26.7/11.3/5.4/5.4/5.4/5.4/9.7/5.4/5.6，问题点与需求分段显示，OPEN/CLOSED 使用状态徽章。启动脚本默认切换 demo4，并同时传入进展／问题两个 Base URL。
- 2026-07-17：扩展 demo4 顶部 show switch：在最左侧新增「会议纪律」「项目概览」两个预留按钮，形成「会议纪律／项目概览／概况／进展／问题」五项顺序；前三项暂置灰，进展与问题交互保持不变。切换区加宽，PM 人名区继续使用横向滑动轴承接剩余宽度。
- 2026-07-17：根据语义梳理收敛 demo4 show switch：合并重复的「项目概览／概况」，最终保留「会议纪律／项目概况／详细进展／遗留问题」四项；前两项暂置灰，「详细进展」承接原进展页，「遗留问题」承接原问题页。
- 2026-07-17：完善 demo4「遗留问题」双周展示：问题 Base 取最近有效 Week 作为当周并同时保留上一周，不按任务状态过滤；上一周遗留问题在上、当周新增问题在下，分别使用独立横向滚动区域与相同十一列宽度；两表间距 32px，表头高度由约 34px 收窄至 26px，内容行高度由 116px 增至 155px。
- 2026-07-17：完成 demo4「会议纪律」视图：启用顶部切换按钮，正文按「会议纪律」与「会议纪律--水果基金」顺序展示两个独立纵向卡片，仅保留卡片内部标题；标题／正文分别使用微软雅黑 24pt／16pt，保留金额红色与缴纳人蓝色强调。真实会议内容存入 gitignore 的 `src/meeting_discipline.json`，模板、静态生成和实时刷新共用同一数据结构，仓库只保留脱敏格式示例。
- 2026-07-17：将 demo4「第 N 周遗留问题／新增问题」分组标题由 16px 放大至 18px，行高同步为 1.4、字重保持 700；使用 `#board .issue-week-title` 覆盖主内容统一 13px 规则，与顶部「TDT 技术项目周例会看板」主标题一致。
- 2026-07-17：完成 demo4「项目概况」第一张图：启用顶部切换按钮，新增「2026年TDT项目价值概况」标题与动态赛道柱状图；Base 云端按「TDT/子任务＝子任务」聚合「赛道」，子任务总数、有效赛道数与柱高随生成动态更新。柱子按参考截图固定顺序展示，不按数量排序，截图外新赛道追加在末尾。
- 2026-07-17：增强 demo4「详细进展」PM 导航：在保留鼠标点击与滑动轴的基础上，新增键盘左右方向键切换；切换按人名条中可点击 PM 的实际视觉顺序执行，端点不循环，不响应组合键或输入控件，并保持当前页面纵向位置。
- 2026-07-18：完成 demo4 代码审查与层级映射修复：记录父阶／子任务只由「TDT/子任务」字段判断（TDT＝父阶、子任务＝子任务），「父记录」仅用于子任务关联所属 TDT；字段筛选改为直接使用全表完整记录，命中子任务无法关联 TDT 时明确报错。同步修复「筛选值＝否」仍误接收 True、无效重复读取目标视图、Base 链接错误回显 token、lark-cli 非 JSON 响应缺少可读错误、scope 配置与模板占位符校验不足等 demo4 生成／刷新链路问题。
- 2026-07-18：完成 demo4「项目概况」第二张卡片：读取多选字段「导入N代产品」，Base 云端按多选组合聚合后在本地展开 16 行小结果，并按记录数归并到 N代基线／CAMON／NOTE／GT／POVA；同一记录中的同品牌不同代际去重计一次。页面在赛道卡片下方新增同宽柱状图卡片，说明文字与柱序按用户确认稿展示，纵轴根据最大值生成五段整洁刻度。
- 2026-07-18：完成 demo4「创新大赛项目概况」第三张卡片：采用左侧统计摘要＋右侧 8 项明细表布局，整体状态由本地变量提供，项目总数及开发中／已完成／取消数量从明细状态自动统计；标题下增加「未链接多维总表」小字。真实项目名称继续存于 gitignore 的 `src/demo_scope.json`，仓库示例仅保留脱敏结构。
- 2026-07-18：调整 demo4 创新大赛区块标题层级：将「创新大赛项目概况」从卡片内的三级标题提升为与「2026年TDT项目价值概况」同级的页面级二级标题，并移到创新大赛卡片上方；统计摘要与明细表内容保持不变。
- 2026-07-19：完成 demo4 周度阶段差异方案的 Base 可行性验证：将用户维护的「上周阶段快照（人工勿动）」作为上周状态，以 lookup「阶段查找」作为当前状态，在「TDT技术项目总表」视图最右侧新增公式字段「阶段对比结果」；只比较统计时点的两份状态，对子任务分类为新增开发中、新增已结项、阶段倒推或数据异常，不追踪周内中间变化。
- 2026-07-19：完成项目级 Skill `siyuan-refresh-tdt-stage-snapshot`：在外部导入刷新「阶段查找」前，对全部子任务执行默认只读预检，经精确确认后按行批量写入「上周阶段快照（自动维护）」和统一的「阶段快照日期」；空阶段同步清空旧快照并提示补充，多值／未知值全量报错并阻止写入，同周重复执行默认拦截，强制覆盖必须提供原因并确认阶段查找尚未刷新，写后回读核对精确时间和源阶段竞态。看板生成／刷新链路继续保持只读。
- 2026-07-19：按 `skills-dev/AGENTS.md` 修正阶段快照 Skill 元数据：`description` 改为以 `Use when...` 开头的纯触发条件，保留 Base URL、TDT、阶段字段等检索关键词；执行限制和排除项移入正文 `TRIGGER / SKIP`，并同步界面元数据。
- 2026-07-19：完成 demo4「2026年TDT项目子任务开发达成概况」末张卡片：使用独立页面标题，左侧并排展示「阶段对比结果」中的新增开发中／新增已结项赛道和子任务名称，右侧按 lookup「状态查找」统计开发中、五类已结项、暂停、取消、驳回，并展示完整达成率公式、`fail＝0` 和「开发完成_进度fail」归属说明；详细进展阶段字段同步由已停用的「项目阶段」切换为「阶段查找」。
- 2026-07-20：将 demo4「创新大赛项目概况」由本地 scope 切换为独立 Base 视图只读数据源：左侧固定展示进行中／已完成／已取消三类数量，风险概述按“全部低风险＝均正常；存在中／高风险＝仅显示对应数量”生成；右侧增加「技术赛道」，项目名称／技术赛道／项目阶段／项目状态／项目风险五列等宽，并拉齐左右区域高度、居中状态统计。
- 2026-07-20：更新 demo4 开发达成概况的 `fail` 口径：项目状态同时包含「开发完成」和「fail」时，既计入已结项，也计入全年预估达成率公式分子的 `fail`；达成率公式及其他状态分类保持不变，页面说明和 README 同步更新。
- 2026-07-20：完善 demo4「演示模式」并收敛模板职责：顶栏固定后使用按实际高度计算的独立占位，展开时不再覆盖主体；2.8 秒无操作自动隐藏后，仅鼠标进入屏幕顶部 18px 感应区才重新展开；四项视图按钮及 PM／问题上下文行统一为 36px 高。演示控制器拆为独立脚本，会议纪律改为模板静态内容，右下角固定显示 `Design by siyuan for TRD`。
- 2026-07-20：统一 demo4 表格字体规则：全局字体锁定微软雅黑，原生表格与九字段 CSS grid 表格共用唯一 token，表头统一 `17px／700`，正文、TDT 名称和合计行统一 `17px／400`；原生表格按 `th／td` 自动继承，九字段 CSS grid 表格按 `role="columnheader"／role="cell"` 自动继承，并用统一高优先级规则阻止组件级样式漂移。详细进展删除父级价值描述卡片，TDT 名称改为横跨整行并按内容自适应高度，下方九字段「价值决策」列保持不变。
- 2026-07-21：部门内优选 Demo4；后续设计收敛、功能修复、正式数据接入和上线验证全部基于 Demo4，Demo1～3 冻结。全部细节确认前继续使用 `demo4` 名称，最终确认后再统一改为正式名称。
- 2026-07-21：完成三个正式 Base 视图接入：进展及项目概况切换到正式 TDT 总表，字段改用「4+4+1 @26」「价值决策」；项目概况改为基于完整分页记录本地聚合，移除普通成员无权调用的 Advanced Base 聚合依赖；遗留问题和创新大赛切换到正式独立视图。
- 2026-07-21：修复遗留问题正序新数据结构：仅填写 `Week` 的记录作为有效空周标记，正确识别第 28 周遗留 1 条、第 29 周新增 0 条；任一周无内容时仍渲染一行 11 列空表格。
- 2026-07-21：更新本地 `start-dashboard.cmd` 的三条正式 Base URL；`start-dashboard.example.cmd` 同步三类正式数据源占位说明，继续避免真实 URL 入库。双击启动与页面内「重新生成」均复用同一组运行参数。
- 2026-07-21：暂停使用项目级 Skill `siyuan-refresh-tdt-stage-snapshot`，阶段快照相关处理改由部门专人手工完成；Skill 的触发条件、执行流程和脚本保持不变，仅备注后续重新启用前须在目标总表中增加「阶段快照日期」字段。经静态检索确认看板生成、服务刷新及启动脚本均不依赖该字段。
- 2026-07-21：完成 Demo4 首轮逐项收敛调整：项目价值概况固定展示 13 个正式赛道并将「存储技术」归入「硬件系统」；创新大赛项目名称列调整为 28%，其余四列保持 18% 等宽；详细进展的价值决策／结项时间列各扩至 7%，控制点改读正式字段「阶段控制点」并逐条显示；遗留问题将表头简化为「子任务名称」，任务与当前进展调整为 19.5% 等宽，任务内容左对齐，「问题点／需求」标签使用 `700` 字重，后七列同步加宽。
- 2026-07-21：按内部评审意见将 Demo4 固定为唯一的 Notion 纯白主题；移除 Claude 暖色、跟随系统的深色主题、右上角主题切换按钮及对应 JavaScript，其他页面功能不变。
- 2026-07-21：修复双击启动后仍展示旧模板：`serve_dashboard.py` 改为每次首页请求重新读取并校验模板；新增 `--replace-existing`，在抓取 Base 前仅替换同项目、同端口的旧看板服务，并关闭对应的旧双击命令窗口。正式版与 example 版启动脚本同步传参并保持纯 CRLF。
- 2026-07-21：修正「2026年TDT项目价值概况」柱状图统计口径：顶部赛道数继续固定显示 13，柱状图恢复为 13 个正式赛道加「降本下沉」「存储技术」共 15 个独立分类；两类子任务不再排除或合并，统计数字与柱子数量不一致不作为错误。
- 2026-07-21：将 Demo4 图表统一改为 Base 字段数据驱动：后端读取字段选项顺序，对全量子任务中的实际非空分类计数，单选逐条计数、多选按选项拆分并在单条记录内去重；前端按返回分类动态生成柱子和对应文字，不再维护固定分类、别名、合并或排除规则。当前两张柱状图分别绑定「赛道」和「4+4+1 @26」，暂无饼状图；「赛道」说明文字暂时固定显示 13，是唯一例外。
- 2026-07-21：细化「4+4+1 @26」柱状图的品牌展示口径：继续从 Base 多选字段动态取值和计数，但将 `GTxx／NOTExx／CAMONxx／POVAxx` 分别归一为 `GT／NOTE／CAMON／POVA`，其他选项保留原名；同一子任务的多个选项归入同品牌时只计一次。
- 2026-07-21：取消详细进展的自动编号列表：控制点、本周进展、下周计划和风险均保留 Base 原文中的人工序号，HTML 使用无列表标记的条目结构，不再自动添加、替换或重排序号；空值只显示 `/`。同步修复两位及以上人工序号被旧正则从数字中间拆开的边界问题。
- 2026-07-21：加强「最新进展描述（TDT PM）」日期段识别：除行首外，兼容中文／英文分号后的合法 `日期：` 作为新段边界，仍按日期值只保留最新段；正文中不带冒号的目标日期和条目日期不分段。风险继续独立读取「项目风险」，仅按人工序号分行。
- 2026-07-21：增加进展标签习惯写法容错：将「本周计划」视为「本周进展」的等价标签，将既有「下周进展」继续视为「下周计划」的等价标签；标签仅用于切分，不进入看板正文，其他日期段和人工序号规则不变。
- 2026-07-21：将开发达成率公式、当前统计口径和备注收纳到达成率数值后的灰色笑脸按钮中，默认隐藏，鼠标悬停或键盘聚焦时以浮层展示；达成率数值和五类状态统计继续常驻。
- 2026-07-22：将原 Demo4 收敛为正式版 `V1.0.0`：模板重命名为 `src/template-dashboard-v1.0.0.html`，页面标题和静态生成／服务端默认参数同步使用正式版本；启动首次抓取和页面内每次重新生成成功后，均按生成时本地日期原子写入 `output/YYYY-MM-DD-tdt-dashboard-v1.0.0.html`，同日覆盖、跨日新建并保留历史文件。
- 2026-07-22：详细进展 PM 滚轴改为严格遵循本地 `demo_scope.json` 的固定 16 人顺序；配置名单存在时不再自动追加名单外的活跃 PM。正式入口只读刷新后，产物内嵌名单为 16 人且顺序、唯一性均通过检查；当前 15 人有汇报项目可点击，1 人无命中项目按既有规则置灰保留。
- 2026-07-22：修正详细进展 PM 滚轴的动态名单规则：固定名单仅作为命中角色的相对排序优先级，不再强制显示未命中角色；名单外 PM 按本次筛选结果中的首次出现顺序追加。四组排序回归覆盖固定成员缺席、乱序输入、名单外多人追加和无固定名单回退；正式双击入口返回 HTTP 200、加载最新模板并更新当天离线 HTML，当前 Base 命中的 15 位固定角色全部显示，1 位未命中角色已隐藏。
- 2026-07-22：将页头年周字号由 `12px` 放大为与主标题一致的 `18px`，其余元信息继续保持 `12px`。正式双击入口返回 HTTP 200，服务页面与当天离线 HTML 均加载新样式，模板 JavaScript 和两份 `.cmd` 的 CRLF 检查通过。
- 2026-07-22：修复进展正文括号内数量被误拆为新条目：从人工序号容错中移除“裸数字＋右括号”，`76)`／`76 )`／`76）`／`76 ）` 均作为正文保留，`1、`／`1.`／`(1)`／`（1）`／带圈序号继续支持。正式双击入口返回 HTTP 200；目标真实子任务由错误的 5 条恢复为 4 条本周进展，数量与后续正文保持在同一条，错误的 `76)` 独立条目为 0。
- 2026-07-22：为页面内「重新生成」增加 `generate_dashboard.py` 按需热更新：刷新前按文件签名检测变化，在隔离候选模块中编译并完成完整 Base 抓取及离线写入，全部成功后才切换运行模块；编译或首次刷新失败时保留旧模块、旧页面数据和旧离线文件。单元回归覆盖未变化复用、无效候选隔离、首次刷新失败不提交和成功提交；正式服务运行中模拟生成脚本变化后，无需重启即由 `/api/data` 返回 HTTP 200 并更新当天产物，目标真实子任务保持正确的 4 条进展。模板变化仍需刷新页面，服务脚本或启动参数变化仍需重启。
- 2026-07-22：详细进展的控制点列表改为全宽、零左缩进并显式左对齐，在保持控制点列宽及其他八列不变的前提下释放可用文字宽度、减少换行。正式双击入口返回 HTTP 200，服务页面与当天离线 HTML 均加载新样式；模板 JavaScript 和两份 `.cmd` 的 CRLF 检查通过。
- 2026-07-22：移除「规划导入产品代分布」柱状图的纵轴刻度与横向网格，取消原轴区左侧留白，仅保留与第一张赛道柱状图一致的底部基线；柱高改为按当前最大品牌数量同比缩放，分类与统计规则不变。正式双击入口更新当天离线 HTML，产物保留 5 个品牌分类且无旧网格结构，模板与产物 JavaScript 检查通过。
- 2026-07-22：将「2026年TDT项目价值概况」第一张赛道柱状图的横轴标签由 `-48deg` 倾斜改为自然水平居中，保持 `14px` 字号及原标签区尺寸不变。正式双击入口返回 HTTP 200，服务页面与当天离线 HTML 均确认无标签旋转且字号不变；模板 JavaScript 和两份 `.cmd` 的 CRLF 检查通过，测试端口已关闭。
- 2026-07-22：将「2026年TDT项目子任务开发达成概况」右侧达成概述的非数据文字直接绑定表格正文共用的字体变量，不再维护独立字号和字重；子任务总数和全年预估达成率两个数据字段继续保持 `23px／700` 及蓝色强调。正式双击入口返回 HTTP 200；浏览器 computed style 对照确认概述文字与表格正文均为微软雅黑、`17px／400`、`zoom: 1` 且无 transform，当天离线 HTML 同步更新，测试端口已关闭。
- 2026-07-22：将正式运行配置从 Demo 数据文件中独立出来：新增本地 `src/dashboard_config.json`，仅保留 16 人 `pm_roster` 固定相对顺序；新增脱敏示例 `src/dashboard_config.example.json`，生成器、服务端及四个启动入口统一改用 `--config`。旧 `--scope` 仅保留为无字段筛选时的兼容白名单入口，旧 `src/demo_scope.json` 已移除且不再承载正式配置。同步修复重复启动时先停旧服务再抓取 Base 造成的无监听窗口：新进程先完成配置校验和首次抓取，再替换旧服务并立即绑定端口；页面仅对网络断连按 `800ms／1600ms` 重试两次，HTTP／Base 明确错误不重试并保留原始原因。
- 2026-07-22：调整项目概况「规划导入产品代分布」文字描述：仅在句首保留一次“规划导入”，后续按 `GT／N代基线／NOTE／CAMON／POVA` 依次展示动态数量，不再逐项重复“导入”；柱状图数据、顺序与统计规则不变。
- 2026-07-22：统一正式版全部表格（含详细进展 CSS grid 表格）的空值显示规则：详细进展、遗留问题、创新大赛和开发达成表中的空字段直接保留为空单元格；整格仅为 `/`、全角斜杠、各类横线或「未填写／暂无／无」时同样清空，正文内部相同字符和文字保持不变。空周问题仍保留完整 11 列空行，所有表格行列与表格位置均不因清空占位内容而删除。
- 2026-07-22：完成 Mac／Claude 视觉训练离线包：从正式模板复制独立副本，内置覆盖会议纪律、项目概况、详细进展、遗留问题及四类风险／长文本／空字段的固化假数据；不包含 Base URL、真实 PM、企业项目数据、lark-cli 或刷新服务。包内提供 `start-preview.command`、校验脚本、Claude 修改边界和 SHA-256 清单，已生成单文件预览及 ZIP；构建、Python 假数据覆盖、模板 JavaScript、空单元格、敏感标识、LF 行尾、ZIP 结构和哈希校验通过，正式模板与运行链路未改。
- 2026-07-27：完成 Mac／Claude 视觉回传集成：正式模板采用 `design.md` 定义的 Numbers 风格中性灰／蓝色 token 与跨平台字体栈；表格统一为 `16px／700／400`，详细进展与连续表格改为单线网格和隔行底色；两张计数柱状图均恢复从 0 起的纵轴与横向网格，`chartScale()` 的最小步长固定为 `1`。正式会议纪律文案保持不变；`design.md` 放入项目根目录、饼图样例归档至 `docs/samples/`，并新增模板视觉回归。三轮审查通过：3 项 Node 回归、12 项 Python 回归、Python 编译、离线产物双脚本／敏感信息／启动脚本 CRLF 检查均通过；正式 `start-dashboard.cmd` 在 8912 端口读取 Base 后首页及 `/api/data` 返回 200，浏览器点击「重新生成」后更新为 90 个详细进展子任务、663 个概况子任务，页面控制台 0 错误，并写出当天离线 HTML。
- 2026-07-27：新增团队《多维总表书写规则》：基于正式 TDT 技术项目总表与 TDT 周例会遗留问题统计的真实字段，说明详细进展九列映射、控制点标准格式、分段和连续两种最新进展写法、总表输入直显的功能宪法，以及遗留问题的 `Week` 双周分组、空周标记和 11 列填写方法。
- 2026-07-29：正式离线看板文件名统一为 `YYYY-MM-DD(WXX)-TDT技术项目状态看板.html`，其中 `WXX` 为两位 ISO 周次。静态生成与页面内刷新共用 `offline_snapshot_filename()`，同日覆盖与跨日保留逻辑不变；`--variant` 保留为旧启动命令兼容参数，不再影响正式文件名。Python 编译与 14 项运行时回归通过；以全新隐藏 `cmd.exe` 启动正式入口在 8931 端口验证首页和 `/api/data` 均返回 200，实际生成 `2026-07-29(W31)-TDT技术项目状态看板.html`，测试服务已停止。
- 2026-07-29：右下角版权标识按正式视觉规范由 `10px` 上调至 `11px`，文案更正为 `Designed by Siyuan for TRD`。更新 `design.md`、正式模板和视觉回归断言；3 项 Node 模板回归与 14 项 Python 运行时回归通过。以正式入口在 8932 端口刷新后，首页及 `/api/data` 均返回 200，当天离线 HTML 已写入新文案和 `11px` 样式，测试服务已停止。
- 2026-07-29：更新「开发达成概况」全年预估达成率口径：已结项严格限定为「已完成／开发完成_正常／开发完成_顺延／开发完成_变更／开发完成_风险／开发完成_技术不可行／开发完成_价值fail／开发完成_进度fail」八类；「延期」与 `fail` 均按「状态查找」是否包含对应关键词统计，中／高风险继续读取「风险状态」，同一状态同时包含「延期」和 `fail` 时分别计入公式分子。灰色说明浮层同步展示当前延期／fail 数、已结项清单和关键词规则。新增运行时回归，覆盖八类已结项、非清单的「开发完成…fail」不再误入已结项以及关键词双重计数；Python 编译、3 项 Node 模板回归和 15 项 Python 运行时回归通过。正式入口在 8933 端口只读刷新后，首页和 `/api/data` 均返回 200，真实统计为开发中 435、已结项 171、分子 11、分母 606、达成率 98.2%，当天离线 HTML 已写入新规则，测试服务已停止。
- 2026-07-29：增加一个仅针对详细进展的容错：「最新进展描述」和「项目风险」中，行首或分号后的紧凑写法 `2.3D`、`1.2.5D` 分别按「序号 2＋正文 3D」「序号 1＋正文 2.5D」输出为 `2. 3D`、`1. 2.5D`，独立 `2D／2.5D／3D／4D` 不会被误判，控制点和其他字段不变。Python 编译和 16 项运行时回归通过。
- 2026-07-29：团队《多维总表书写规则》将英文正文超链接提示合并到「本周进展和下周计划统一写法」：说明这是多维表格的自动识别特性，明确禁止无空格写法 `1.ID`，并列出 `1. ID`、`1、ID`、`1、 ID` 三种正确写法；中文顿号不会触发超链接，英文句点后必须加半角空格。文档内容与 Markdown diff 检查通过。
- 2026-07-30：核验正式总表字段「4+4+1 @26」的元数据为 `type=select` 且 `multiple=true`，确认为多选；产品代聚合继续逐项计数、同一子任务同一归一品牌只计一次，并新增回归覆盖。项目概况年份改由生成时日历年份 `dashboardYear` 驱动，所有年份文字使用 Numbers 蓝；跨年边界测试确认 `2027-01-01` 即使 ISO 周仍属 2026，页面年份仍为 2027。右下角 `Designed by Siyuan for TRD` 升至 `12px`。3 项 Node 回归、Python 编译和 18 项 Python 回归通过；正式双击入口在 8942 端口返回首页及 `/api/data` HTTP 200，实时结果为 2026／W31、5 个产品代分类，GT 与 POVA 均存在，测试服务已停止。
- 2026-07-30：收窄看板字体兜底规则：`#board` 保留 `13px／400` 根级默认值，移除对子元素的通配符强制写入；详细进展 PM 摘要与视图级空态明确使用 `16px／400`，图表纵轴刻度继续保持 `13px`。项目概况年份恢复继承所在标题／概述的字号和字重。同步将 README、设计规范、历史说明和测试诊断中的九字段「卡片式表格」更正为 Numbers 风格连续 CSS grid 表格。3 项 Node 回归、Python 编译和 18 项 Python 运行时回归通过；正式入口在 8943 端口验证首页及 `/api/data` 返回 HTTP 200，命中 15 个 PM 分组，刷新产物与当天离线 HTML 均确认加载新规则，测试服务已停止。

- 2026-07-31：基于 `ca2d9d6` 的正式视觉规范／进展容错功能和 `11296d5` 的项目概况统计／字体继承修正，将正式看板由 `V1.0.0` 升级为 `V1.1.0`。新增并启用 `src/template-dashboard-v1.1.0.html`，保留 `V1.0.0` 兼容模板；Python 默认参数、HTTP Server 标识、Windows／macOS 四个启动入口、项目规范、视觉规范、README 和测试目标全部同步到新版本。
- 2026-08-12：彻底解决与02评审专家打分系统的后台端口及浏览器旧页面冲突。初次仅分离后台身份后，用户在Chrome的历史8765标签页仍保留02旧DOM，旧DOM向新的01服务请求 `/static/styles.css` 得到404，表现为8765显示无样式的02页面。01现停用历史共用端口8765，固定项目身份 `tdt-project-status-dashboard` 和全新专属端口段 `8710—8719`；启动URL携带当前 `build_id` 强制浏览器重新导航。27项Python运行时回归、3项Node模板回归、Python编译和启动脚本行尾通过；真实HTTP及浏览器验收确认8710显示完整Numbers风格V1.2.0看板，8865同时显示02完整打分系统，8765已停止监听，各自重复启动只复用本项目。

## 进行中

- 妙搭团队试点准备：全栈迁移与创建者发布态验收已完成；下一步等待专用只读飞书应用开通并替换临时 CLI 应用身份，复验后再按用户授权安排团队试点。
- 妙搭开发期不添加协作者或其他操作者，不扩大运行时可见范围；团队试点延后至 2026 年 8 月第一周由用户另行授权。
- 基于正式版 `V1.3.0` 继续逐一核验项目概况、详细进展和遗留问题的内容有效性、异常值、交互与视觉细节。
- 正式进展源当前产生 19 条进展格式警告，需结合 Base 原文逐条判断是源数据格式问题还是解析缺陷。
- 等待部门使用正式版 `V1.3.0` 继续确认整体效果；当前阶段柱状图只验收统计准确性。
- Demo1～3 保持冻结。

## 后续事项

- 妙搭创建者验收通过后，安排团队试点，验证普通负责人无需本地环境即可打开和刷新；届时再评估协作者角色与可见范围。
- 内部演示收集反馈；确认后评估扩大收录范围（全部进行中子任务）。
- 设计细化：将配色 token 与组件同步到 claude.ai/design 设计系统项目精修（DesignSync）。
- 评估升级为可分发 skill（`skills-dev/`）。

## 阻塞

- 妙搭正式最小权限身份待配置：当前使用既有 CLI 应用身份完成创建者验证；专用只读飞书应用仍待管理员开通。该事项不阻塞当前技术链路验证，但在团队试点前必须替换并重新执行只读验收。
- Mac／Claude 视觉训练包向跨租户私人飞书发送受阻：通过联系人 `user_id` 发送返回飞书错误 `230038`（禁止跨租户 P2P 操作），改用已有 `chat_id` 返回 `230027`（当前用户授权或租户策略拒绝）。本机企业飞书客户端可作为替代发送入口，但 Windows UI 的最终发送动作需要用户在操作时确认并通过应用控制授权。

## 最近验证

- 2026-08-02：用户完成妙搭开发页与线上发布页双端人工复验，数据抓取、看板生成和离线下载均成功；随后通过妙搭观测接口查询近两小时线上运行状态，ERROR 与 WARN 日志列表均为空，requests 与 latency 指标均未返回采样点。App 仓库文档提交钩子通过，形成 `4da4c7d`；本次没有修改运行代码、环境变量或 release。
- 2026-08-02：正式 Base 切换发布与线上验收通过。`npm run verify:release` 完成 lint、服务端／客户端类型检查、生产构建和 5 个 Jest 套件／21 项测试，所需的 5 类部署产物全部存在，仅保留既有的前端单块超过 800 kB 提示。release `7669229089128172732` 返回 `finished`、无发布错误并绑定 App 完整提交 `18197962bea154305cb07157097a4a08cbe9f43f`。创建者线上页面首次生成及再次「重新生成」均返回 2026 年 W31 正式数据：664 个概况子任务、98 个详细进展子任务、创新大赛 9 项、上一周／本周遗留问题各 1 条；刷新时间由 08:55 更新为 08:56。下载产物 `2026-08-02(W31)-TDT技术项目状态看板 (19).html` 为 139425 字节，含正式数据且外链脚本／样式均为 0；最近 15 分钟线上 ERROR 日志为空。
- 2026-08-01：妙搭开发沙箱 Git 恢复验证完成。App 提交 `1819796` fast-forward push 后，`git ls-remote` 返回完整 SHA `18197962bea154305cb07157097a4a08cbe9f43f`；云端先执行 `git merge --abort`，再 fetch 并 hard reset 到 `origin/sprint/default`，最终分支差异为 `+0/-0`，无未合并文件或冲突标记，仅保留平台生成且未删除的未跟踪目录 `server/capabilities/`。NestJS 重启后日志确认首页和 `GET /app/app_17b7awscgpe/api/data` 路由均已注册，首页 curl 为 `HTTP 200`；命令行数据请求因缺少浏览器 CSRF Cookie 返回 `HTTP 403`。登录 Chrome 中开发页仍使用旧预览实例，Vite HMR 未连接且整页重载超时，因此开发页数据刷新未通过；已停止完成修复后自动续跑的妙搭 AI 任务，未发布、未修改 `main`、未 force-push。
- 2026-08-01：妙搭平台脚手架同步修复本地门禁通过。真实 MinGit Hook 首轮复现新版脚手架经 WSL 调用 `C:/Program Files/nodejs/npm` 失败，恢复 `node.exe` Hook 和 Node npm CLI 启动方式后通过；`npm run lint` 的 ESLint、服务端／客户端 TypeScript 和 Stylelint 全部返回 0。`npm run build` 在 269.1 秒内完成，静态分析 264 个包、递归补充 148 个包，最终复制 412 个生产依赖且失败 0；`dist/run.sh`、`dist/package.json`、`dist/node_modules/`、`dist/server/main.js` 和 `dist/dist/client/index.html` 均通过门禁。5 个 Jest 套件／21 项测试全部通过，仅保留既有前端单块超过 800 kB 提示；App 提交为 `1819796`，现已推送到 `sprint/default`，未发布。
- 2026-08-01：妙搭 App 创建者发布态验收通过。`sprint/default` 以 fast-forward 从 `01f10dc` 推进到 `8ca2c8e`，release `7669059896394321192` 返回 `finished` 并明确绑定该提交；妙搭服务端将发布态 `main` 推进为合并提交 `e8391da`，未再出现 `/opt/bytefaas/run.sh` 缺失或平台内部 DNS 502。用户在创建者登录会话中确认首页、首次生成和重新生成均成功；下载得到 `2026-08-01(W31)-TDT技术项目状态看板.html`，大小 139460 字节，SHA-256 为 `2F961526EE4F1D73AA6AF001865FED53415E29C12247E605049F1D58BAE8F056`。离线 DATA 可解析，得到 2026 年 W31、15 个 PM 分组、98 个详细进展项目、664 条概况记录、9 条创新大赛记录、上一周 1 条／本周 1 条遗留问题和 2 个会议区块；`V1.2.0`、会议内容均存在，数据占位符已替换，外部脚本和样式依赖均为 0。最近 20 分钟线上 ERROR 日志为空；观测平台暂未返回 `/api/data` 请求明细或指标，不影响由实际刷新与下载产物确认的功能结果。
- 2026-08-01：妙搭部署产物契约修复验证通过。定向回归确认 `dist/run.sh`、`dist/package.json`、`dist/node_modules/`、`dist/server/main.js` 和 `dist/dist/client/index.html` 均存在，旧的 `dist/server/run.sh`、`dist/server/package.json` 和 `dist/server/node_modules/` 均不存在；`scripts/run.sh` 为 LF，Git 模式为 `100755`。`npm run verify:release` 完成 lint、类型检查、生产构建和 5 个 Jest 套件／21 项测试，全部通过，仅保留既有的前端单块超过 800 kB 提示。编译入口从 `dist/` 根目录实际执行到 Nest 初始化，随后因本机没有妙搭运行时注入的 `FORCE_AUTHN_INNERAPI_DOMAIN` 停止；线上运行与 DNS 状态待新 release 验证。
- 2026-08-01：断电后双仓库状态与妙搭 Git 同步验证通过。父仓库与 App 仓库 `git fsck` 均返回 0，工作树干净，无 index lock、merge、rebase、cherry-pick 或 revert 残留。App 本地 6 个提交与远端初始化提交共有基线 `22f1045`；使用 `ours` 策略生成合并提交 `01f10dc`，两个父提交分别为功能提交 `9f63ecb` 与远端模板提交 `c97865c`，合并前后 tree SHA 完全一致。随后 push 到 `origin/sprint/default`，远端 SHA 与本地 `01f10dc` 完全一致；未 force-push、未修改 `main`、未发布。
- 2026-08-01：`V1.2.0` 双端版本升级验证通过。父仓库 21 项 Python 回归、3 项 Node 模板回归和 Python 编译全部成功；两份 `.cmd` 为纯 CRLF、两份 `.command` 为纯 LF。以全新隐藏 `cmd.exe` 启动正式入口，首页返回 `HTTP 200`，标题和主标题均为 `V1.2.0`；真实副本数据保持 15 个 PM 分组、98 个详细进展项目、664 条概况记录、9 条创新大赛记录、W30／W31 各 0 条问题及 2 个会议区块。当日离线单文件已覆盖输出为 `output/2026-08-01(W31)-TDT技术项目状态看板.html`，版本为 `V1.2.0` 且无数据占位符；测试服务已停止。App 仓库 lint、类型检查、14 项 Jest、生产构建及近期日志检查通过，仅保留既有的前端单块超过 800 kB 性能提示；App 提交为 `9f63ecb`，尚未 push 或发布。
- 2026-08-01：妙搭 App 创建者本地端到端验证通过。既有 CLI 应用获取新 `tenant_access_token` 后，三张副本数据表的 `record_search` 均为 `HTTP 200／code 0`；前端经 8080 妙搭本地代理访问首页和 `/api/data` 均为 `HTTP 200`，返回 2026 年 W31、15 个 PM 分组、98 个详细进展项目、664 条概况记录、9 条创新大赛记录和 1 条遗留问题，11 条既有数据格式警告中 Lookup 相关为 0。Jest 7 项、`npm run lint`、`npm run type:check` 与 `npm run build` 全部通过，客户端及服务端日志无新错误；构建仅保留既有的前端单块超过 800 kB 性能提示。真实 URL、凭据和值均未写入 Git；专用只读应用替换、妙搭远端环境配置、push 和发布尚未执行。
- 2026-07-31：`V1.1.0` 版本升级验证通过。3 项 Node 模板回归、18 项 Python 运行时回归和 Python 编译均成功；版本测试确认新模板标题与主标题均为 `V1.1.0`，旧版本号未残留。两份 `.cmd` 保持纯 CRLF、两份 `.command` 保持纯 LF。以全新隐藏 `cmd.exe` 模拟双击正式 `start-dashboard.cmd --no-open --port 8951`，首页返回 HTTP 200，标题和主标题均显示 `V1.1.0`；当天离线快照已更新为 `2026-07-31(W31)-TDT技术项目状态看板.html`，数据占位符无残留；测试服务已停止。
- 2026-07-10：通过 PowerShell 检查五个计划文件均存在，文件集合准确且无空目录。
- 2026-07-10：通过 `shared/scripts/git.cmd status --short --untracked-files=all` 检查项目改动范围，仅包含五个计划文件。
- 2026-07-11：lark-cli 只读验证数据源可达：视图「TDT技术项目总表」184 条记录，字段清单确认（`项目阶段`、`项目状态` 不在视图可见列，需全表补读）。
- 2026-07-11：`generate_dashboard.py` 真实运行成功，产物 `output/2026-07-11-tdt-dashboard.html` 与 `.report.json`；两条抽样记录逐字段核对通过。
- 2026-07-15：demo4 Review 1 逐项通过顶部视图、PM 唯一正文展示、无侧栏、TDT 双卡、共享字段头、九字段比例、日期格式和窄屏可读宽度检查。
- 2026-07-15：demo4 Review 2 通过四版模板 JavaScript/占位符契约、Python 注入与脚本边界转义、真实数据 19 个子任务/3 个 PM/同 TDT 多子任务、AI 刷新/静态回退/双配色/PM 切换、Demo 1～3 回归和 Git diff 检查；真实数据保留 2 条进展格式源警告。
- 2026-07-16：demo4 排版复核逐项通过概览小标题移除、正文直接输出、微软雅黑 13px 统一继承、全字段垂直居中和前五列水平居中；真实生成 19 个子任务/3 个 PM，四版模板 JavaScript 与 Git diff 检查通过。
- 2026-07-16：demo4 字体/日期/行距复核通过：生成产物中主内容仅 TDT 技术项目名称为粗体，其余均为微软雅黑 13px 常规字重；19 个子任务的日期均符合 YY/MM/DD 或空值 `/`，进展/计划列表为 13px 行高与 13px 条目间隔；Demo 1～3 回归和 Git diff 检查通过。
- 2026-07-16：demo4 列宽复核通过：模板与生成产物均为 10/6/6/6/7/14/20/20/11，总和 100%，旧比例未残留；真实生成仍命中 19 个子任务、3 个 PM，模板及产物 JavaScript 解析、Demo 1～3 无改动检查和 Git diff 检查通过；保留原有 2 条进展格式源警告。
- 2026-07-16：demo4 要点间隔复核通过：模板与生成产物均对进展、计划、风险列表应用 13px 行高和相邻项 26px 间隔，旧 13px 项间距规则已移除；最新生成仍命中 19 个子任务、3 个 PM，JavaScript 解析与 Python 编译检查通过；保留原有 2 条进展格式源警告。
- 2026-07-16：demo4 问题页 Review 1 通过：真实生成命中进展 19 个子任务／3 个 PM；问题 Base 选择 2026 · W28 的 1 条有效记录，状态 OPEN、计划完成日期 7月16日；十一列顺序与列宽总和 100%，`Week` 未进入表格，生成 HTML 未包含 Base URL。
- 2026-07-16：demo4 问题页 Review 2 通过：Chrome 1600×900 实际点击「AI 看板」与「问题」完成切换，确认 PM 轴隐藏、周次／状态摘要呈现、连续边框与上下居中、问题点／需求分段、日期字段头单行显示；本地服务 `/api/data` 实时刷新返回 HTTP 200，并再次得到 19 个进展子任务／3 个 PM／W28 的 1 条 OPEN 问题；Python 编译与模板 JavaScript 解析通过，Windows 启动脚本以全新 cmd 进程执行 `--help` 通过，`.cmd` 为纯 CRLF、`.command` 为纯 LF。
- 2026-07-17：demo4 五项 show switch 复核通过：按钮顺序为「会议纪律／项目概览／概况／进展／问题」，前三项禁用、进展当前高亮、问题可点击；1600px 下五个按钮各 92px，PM 轴可视宽 935px、内容宽 995px，1100px 下可视宽 572px 且保持横向滚动。真实数据重新生成 19 个进展子任务／3 个 PM／W28 的 1 条 OPEN 问题，Python 编译、模板 JavaScript 解析与浏览器截图检查通过。
- 2026-07-17：demo4 四项 show switch 复核通过：生成 HTML 中按钮顺序为「会议纪律／项目概况／详细进展／遗留问题」，前两项禁用，后两项继续复用原进展／问题交互；切换区改为四等分，PM 轴保持横向滚动。真实数据重新生成 19 个详细进展子任务／3 个 PM／W28 的 1 条遗留问题，模板与生成产物 JavaScript 解析、Python 编译及 HTML 结构检查通过；按用户要求未生成 PNG。
- 2026-07-17：demo4 遗留问题双周逻辑验证通过：目标视图完整读取 7 条记录（`has_more=false`），忽略全空占位后得到第 27 周 1 条 CLOSED、第 28 周 2 条 OPEN；重新生成 `output/2026-07-17-tdt-dashboard-demo4.html`，报告分组、双表运行时模板、26px/155px 高度、32px 间距、Python 编译、模板及产物 JavaScript 解析和 Base URL 不入产物检查通过。浏览器插件核心脚本缺失，未完成自动化视觉检查，等待用户打开产物确认。
- 2026-07-17：demo4 会议纪律数据与结构验证通过：重新生成产物仍命中 19 个详细进展子任务／3 个 PM，第 27 周 1 条遗留问题与第 28 周 2 条新增问题；会议纪律报告为启用状态，包含 2 个独立卡片与 2／7 条规则；Python 编译、模板及产物 JavaScript 解析、会议按钮启用、真实会议内容不进入 Git、Base URL／token 不进入产物检查均通过。浏览器安全策略禁止自动打开 `file://` 页面，未完成自动视觉检查，等待用户打开产物确认。
- 2026-07-17：demo4 双周分组标题字号修复后复核通过：初版 `.issue-week-title` 被 `#board *` 的 13px／400 规则覆盖；改为更高优先级的 `#board .issue-week-title` 后，模板与生成产物均实际采用 18px／1.4／700，真实数据仍为第 27 周 1 条、第 28 周 2 条，未改变双表结构、列宽、行高和数据规则。
- 2026-07-17：按参考截图将 demo4 会议纪律正文由 14pt 调整为 16pt，正文／24pt 标题比例由 58.3% 提升至 66.7%，更接近截图约三分之二的视觉比例；模板与生成产物均确认标题 24pt、正文及其子节点 16pt，旧会议正文 14pt 无残留。重新生成后详细进展仍为 19 个子任务／3 个 PM，遗留问题仍为第 27 周 1 条、第 28 周 2 条，Python 编译、模板与产物 JavaScript 解析及 Git diff 检查通过。
- 2026-07-17：demo4 项目概况验证通过：Base 云端动态聚合得到 654 个子任务、15 个有效赛道；生成产物按「硬件系统／软件系统／颜值／通信／充电／功耗温升／显示／游戏／相机／耐用性／AI／AIoT／研发测试」固定顺序展示，并将「降本下沉／存储技术」追加在末尾。项目概况按钮已启用，标题、动态摘要、柱状图数据和无占位符检查通过；Python 编译、模板与产物 JavaScript 解析通过，详细进展 19 个子任务与双周遗留问题数据保持不变。
- 2026-07-17：demo4 PM 键盘导航验证通过：独立行为测试确认左右键按可点击按钮的视觉顺序移动、端点不循环，键盘切换调用 `renderBoard(false)` 保持页面位置；模板与生成产物均包含方向键快捷键声明和受「详细进展」视图约束的 `keydown` 处理，JavaScript 解析通过。重新生成后仍为 19 个详细进展子任务、654 个概况子任务、15 个有效赛道和双周 3 条遗留问题。
- 2026-07-18：demo4 层级映射与代码审查回归通过：真实 Base 元数据确认「TDT/子任务」为含 TDT／子任务两个选项的 select，「父记录」为指向同表的 link；针对性测试覆盖带父记录关联的 TDT 仍识别为父阶、非 TDT 记录不能作为父阶、是／否与 True／False 正反筛选、token 脱敏及重复模板占位符拒绝。真实数据重新生成 `output/2026-07-17-tdt-dashboard-demo4.html`，19 个详细进展子任务均解析到 TDT 父阶，项目概况 654 个子任务／15 个赛道，双周遗留问题 3 条；demo4 模板与产物 JavaScript、Python 编译、示例 JSON、输出无 Base URL／token 和 Git diff 检查均通过，保留原有 2 条进展格式源警告。
- 2026-07-18：demo4 项目概况第二张卡片真实数据验证通过：字段「导入N代产品」确认为多选，云端返回 16 个多选组合；展开累计得到 N代基线 24、CAMON 47、NOTE 11、GT 47、POVA 44，未出现未归类选项。重新生成 `output/2026-07-18-tdt-dashboard-demo4.html`；第一张卡片仍为 654 个子任务／15 个赛道，详细进展仍为 19 个子任务／3 个 PM，双周遗留问题仍为 3 条。Python 编译、产品代聚合纯函数回归、demo4 模板及产物 JavaScript、动态文案与五柱结构、输出无 Base URL／token 和 Git diff 检查均通过，保留原有 2 条进展格式源警告。
- 2026-07-18：demo4 创新大赛第三张卡片验证通过：本地配置装配为总数 8、开发中 5、已完成 3、取消 0、整体状态正常，8 项明细及「未链接多维总表」提示均进入生成数据与页面结构。重新生成 `output/2026-07-18-tdt-dashboard-demo4.html` 后，Base 动态数据仍为 654 个子任务／15 个赛道、N代基线 24／CAMON 47／NOTE 11／GT 47／POVA 44、详细进展 19 个子任务／3 个 PM、双周遗留问题 3 条；Python 编译、模板及产物 JavaScript、JSON／报告统计、输出敏感信息扫描均通过，保留原有 2 条进展格式源警告。
- 2026-07-18：demo4 创新大赛标题层级验证通过：模板与重新生成的产物均使用 `h2.project-overview-title` 展示「2026年TDT项目价值概况」和「创新大赛项目概况」，旧的卡片内三级标题已移除；产物内嵌 JavaScript 语法通过，Base 动态数据仍为 654 个子任务／15 个赛道、N代基线 24／CAMON 47／NOTE 11／GT 47／POVA 44，双周遗留问题仍为 3 条，保留原有 2 条进展格式源警告。
- 2026-07-19：阶段对比全量检测完成：Base 云端聚合 654 条子任务，识别新增开发中 5 条、新增已结项 4 条、阶段倒推 26 条、当前阶段为空 7 条，其余 612 条不展示；字段创建 dry-run、公式归一化、目标视图末列可见性和四类明细回查均完成，未改写任何现有业务记录或阶段快照值。
- 2026-07-19：阶段快照 Skill 验证通过：`quick_validate.py`、两份 Python AST 语法检查和缺失 lark-cli 受控错误检查通过；完成两轮代码 Review，修复同周漏写／外部导入竞态校验、Windows `.cmd` URL 引号和 CLI 启动异常处理；无网络模拟覆盖正常复制、空值清空、精确时间回读、同周拦截、带原因强制覆盖与多值异常。最终真实 Base 只读预检识别 654 条子任务、646 条非空阶段、8 条空阶段、0 条本周快照日期，阶段分布为概念 400／计划 40／开发 140／技术迁移 66；未执行正式写入。
- 2026-07-19：demo4 开发达成概况真实数据验证通过：完整分页读取 654 个子任务，得到开发中 437、已结项 161、暂停 28、取消 19、驳回 1、未分类 8；新增开发中 5、新增已结项 4；风险分子为中风险 8／高风险 0／延期 0／fail 0，分母 598，达成率 98.7%。重新生成 `output/2026-07-19-tdt-dashboard-demo4.html`，Python AST、统计纯函数、模板及产物 JavaScript、内嵌数据、完整公式、五类已结项定义、Base URL 不入产物和 Git diff 检查均通过；保留原有 2 条进展格式源警告。浏览器安全策略禁止自动打开 `file://`，自动视觉检查未完成，等待用户直接打开产物确认。
- 2026-07-20：demo4 会议纪律紧凑排版验证通过：模板与 `output/2026-07-19-tdt-dashboard-demo4.html` 的会议专用 CSS 完全一致，正文由 16pt 调整为 15pt、行高由 1.65 调整为 1.55，标题保持 24pt；同步压缩卡片内边距、标题／条目／卡片间距与页面底部留白，其他视图样式未改。静态检查确认两段会议纪律内容仍完整，旧 16pt 会议正文规则无残留；浏览器安全策略禁止自动读取 `file://` 页面，需以用户浏览器 100% 缩放做最终视觉确认。
- 2026-07-20：demo4 创新大赛 Base 接入验证通过：目标视图完整读取 8 条记录（进行中 5／已完成 3／已取消 0，低风险 8／中风险 0／高风险 0），生成文案为“均正常”；高风险 1＋已取消 1 的模拟回归得到 5／2／1 和“其中高风险项目1个”。重新生成 `output/2026-07-20-tdt-dashboard-demo4.html`，五列 20% 等宽、左右 stretch、状态统计居中、旧“未链接多维总表”无残留；Python AST、模板与产物 JavaScript、示例 JSON、启动脚本参数及行尾、Base URL／token 不入产物检查均通过。浏览器安全策略禁止读取 `file://`，最终视觉效果等待用户直接打开产物确认。
- 2026-07-20：demo4 `fail` 双重计数口径验证通过：针对性回归覆盖标准「开发完成_进度fail」和自定义大小写 `FAIL` 状态，确认均同时进入已结项和公式分子；实时完整读取 654 个子任务后，开发中 437、已结项 161、暂停 28、取消 19、驳回 1、未分类 8 保持不变，`fail` 由 0 更新为 2，风险分子由 8 更新为 10，分母仍为 598，全年预估达成率由 98.7% 更新为 98.3%。重新生成 `output/2026-07-20-tdt-dashboard-demo4.html`，Python 编译、统计纯函数、页面新口径、内嵌数据、产物 JavaScript 和敏感信息扫描均通过；保留原有 2 条进展格式源警告。
- 2026-07-20：demo4 演示模板重构验证通过：Python 编译、模板与重新生成产物的两个独立 JavaScript 模块语法、静态会议纪律完整性、`DATA.meetingDiscipline` 解耦、36px 导航统一高度、页头实际高度占位、顶部 18px 唤出条件和版权标识检查均通过；DOM 状态模拟确认空闲自动隐藏、中部鼠标移动不唤出、顶部移动恢复以及生成／加载按钮状态同步。实时数据保持 654 个子任务、`fail＝2`、达成率 98.3%，双周问题仍为第 27 周 1 条／第 28 周 2 条；保留原有 2 条进展格式源警告。本机 Browser 插件缺失内部说明文件，未能完成自动化全屏视觉检查，需用户在 Chrome／Edge 中打开产物确认实际全屏效果。
- 2026-07-20：demo4 统一表格字体回归通过：模板与重新生成产物中 `17px／700／400` 三个表格 token 均只定义一次，原生表格、ARIA CSS grid 表格、TDT 名称和合计行全部由统一选择器继承；静态审计确认详细进展、遗留问题、创新大赛、开发达成概况不存在组件级字号或字重覆盖。详细进展每个 TDT 顶部只保留一个全宽名称卡片，父级价值描述不再渲染。Python 编译、模板与产物双脚本语法、Base URL 敏感信息扫描均通过；实时数据保持 654 个子任务、`fail＝2`、达成率 98.3%，双周问题仍为第 27 周 1 条／第 28 周 2 条，保留原有 2 条进展格式源警告。
- 2026-07-21：正式数据源全链路验证通过：完整分页读取 662 个子任务，项目概况得到 15 个赛道，产品代分布为 N代基线 24／CAMON 47／NOTE 27／GT 46／POVA 44；详细进展按「是否上周三例会＝是」命中 67 个子任务／12 个 PM；开发达成统计为开发中 436／已结项 169，预估达成率 98.0%；创新大赛读取 9 项（进行中 7／已完成 2／已取消 0，均为低风险）；遗留问题识别第 28 周 1 条／第 29 周 0 条。生成产物及报告成功，Python 编译、两个内嵌 JavaScript、Base URL／token 不入产物、空周占位和纯 CRLF 启动脚本检查通过。
- 2026-07-21：以全新隐藏 `cmd.exe` 进程模拟双击 `start-dashboard.cmd --no-open --port 8876`，首页返回 HTTP 200；随后请求 `/api/data` 再次完整读取三个正式 Base，返回项目概况 662、遗留问题 28周 1／29周 0、创新大赛 9，确认页面内「重新生成」使用与启动时相同的正式数据源。测试结束后已停止该进程树。
- 2026-07-21：Chrome 本地页面视觉核验通过：实际点击「AI 看板」完成一次重新生成并切换到「遗留问题」；第 28 周显示 1 条真实问题，第 29 周显示 1 行完整的 11 个空单元格，实测行高 155px、单元格边框为实线且连续，页面控制台无错误。测试标签页与 8877 本地服务均已关闭。
- 2026-07-21：阶段快照自动化暂停检查通过：项目规范、README 与 Skill 文档均明确暂停及人工处理边界，Skill 元数据、执行流程和脚本未改；`src/generate_dashboard.py`、`src/serve_dashboard.py` 和启动脚本未引用「阶段快照日期」或阶段快照脚本，看板只读链路无需随总表字段删除而修改。
- 2026-07-21：Demo4 首轮收敛真实数据与浏览器验证通过：正式数据得到 662 个概况子任务、13 根固定赛道柱，「存储技术」合并后硬件系统为 95 个，非正式源值「降本下沉」不单独成柱；创新大赛实际列宽为 252／162／162／162／162px；详细进展日期列约 81px、`nowrap` 生效且 0 个日期换行，正式字段「阶段控制点」样例拆为 2 个独立列表项；遗留问题任务／当前进展均为 269px，任务左对齐，两个标签字重均为 `700`，空周占位仍保留，浏览器控制台 0 错误。测试页面和 8878 本地服务已关闭。
- 2026-07-21：Demo4 单主题验证通过：模板 JavaScript 解析和 Python 编译通过，静态检索确认不再包含 palette、Claude 暖色、`data-palette` 或系统深色媒体查询；浏览器实际背景为 `rgb(255, 255, 255)`，右上角只保留「AI 看板／演示模式」，主题按钮数量为 0，控制台 0 错误。测试页面和 8878 本地服务已关闭。
- 2026-07-21：双击入口更新验证通过：服务模板连续读取测试确认不再缓存；example 脚本占位拦截正常；两份 `.cmd` 均为 UTF-8、纯 CRLF。使用默认 `8765` 连续模拟两次双击，第一次服务 PID `37652` 被第二次启动替换为 `14628`，第一次命令窗口同步退出；第二次首页确认加载当前纯白模板且无主题按钮。测试服务与命令窗口均已关闭。
- 2026-07-21：价值概况柱状图拆分验证通过：纯函数回归确认固定赛道数为 13、柱状图分类为 15，「硬件系统／降本下沉／存储技术」分别独立计数；以全新隐藏 `cmd.exe` 模拟双击正式脚本并请求 `/api/data`，实时读取 663 个子任务，15 根柱子合计 663、未归类 0，其中硬件系统 92、降本下沉 12、存储技术 3。Python 编译通过，测试服务已关闭。
- 2026-07-21：图表数据驱动正式数据验证通过：字段元数据确认「赛道」为 17 个备选项的单选字段、「4+4+1 @26」为 5 个备选项的多选字段；纯函数覆盖新增分类自动追加、未使用选项不成柱、多选拆分及单记录去重。以正式脚本启动独立 8879 服务并请求 `/api/data`，赛道文字固定 13、实际生成 15 根柱且合计 663、未填写 0；产品代按 Base 原始选项生成 GT 60 46／N代基线 24／Note 70 28／Camon 60 47／Pova 9 44。Python 编译和模板双脚本语法通过，测试服务已关闭。
- 2026-07-21：产品代品牌归一验证通过：纯函数覆盖大小写不敏感的 GT／NOTE／CAMON／POVA 前缀匹配、非目标选项原名保留及同记录同品牌去重；正式脚本独立启动并请求 `/api/data` 后，第二张图输出 GT 46／N代基线 24／NOTE 28／CAMON 47／POVA 44，第一张图仍为文字 13、实际 15 根柱。Python 编译通过，测试服务已关闭。
- 2026-07-21：详细进展人工序号验证通过：纯函数确认控制点、本周进展和下周计划均原样保留 `1／2／10／11` 等人工序号，模板双脚本语法通过且不存在详细进展 `<ol>` 自动列表。正式脚本启动并刷新 93 个子任务后，共识别 431 条人工编号内容、6 条两位数编号内容，`10／11／12／13` 均完整保留，被拆成单独数字的异常条目为 0；服务模板确认使用无标记列表，测试服务已关闭。
- 2026-07-21：旧日期段容错验证通过：读取正式 Base 中李文瑞「二次元数字形象」原文，确认旧内容以同一行 `；7/17：` 开始。精确样例与正式 `/api/data` 刷新均确认输出不再包含 `7/17` 和“3D数字人采购申请已提交”，最新段内的目标日期 `7/30` 与条目日期 `7/24` 均保留；Python 编译通过，测试服务已关闭。
- 2026-07-21：进展标签等价容错验证通过：纯函数覆盖「本周进展＋下周计划」和「本周计划＋下周进展」两组写法，并确认旧日期排除不回退。正式刷新李文瑞「二次元数字形象」后，本周列仅保留 `1.数字人采购进度跟进--李文瑞，7/24；`，不再显示「本周计划：」标签；下周内容、目标日期 `7/30` 正常，`7/17` 仍未进入输出。Python 编译通过，测试服务已关闭。
- 2026-07-21：开发达成率说明浮层验证通过：模板两个 JavaScript 模块和 Python 编译通过；精确样式断言确认笑脸按钮默认使用浅灰边框与灰色图标，悬停改为浅灰底和深灰图标，公式／统计口径／备注默认隐藏且支持悬停与键盘聚焦显示。运行中 8880 首页返回 HTTP 200 并读取最新模板；两份启动脚本继续使用 Demo4、三类正式数据源参数、`--replace-existing` 和纯 CRLF。
- 2026-07-22：正式版命名与离线分发链路验证通过：旧 `src/template-demo4.html` 已不存在，正式模板、页面标题、Python 默认参数及四个启动入口统一为 `V1.0.0`；纯函数回归确认同日覆盖、跨日新建、数据内嵌和临时文件无残留。以全新隐藏 `cmd.exe` 模拟双击正式脚本启动 8881 服务，首次抓取生成 `output/2026-07-22-tdt-dashboard-v1.0.0.html`，随后请求 `/api/data` 返回 15 个 PM 分组／663 个概况子任务并确认当天文件写入时间更新；产物包含两个可解析的内嵌 JavaScript、无外部静态资源、无数据占位符和 Base URL，且被 Git 忽略。两份 `.cmd` 为纯 CRLF，两份 `.command` 为纯 LF，测试服务已关闭。
- 2026-07-22：正式配置迁移与刷新稳定性验证通过三轮 Review。第一轮核对配置、命令行和四个启动入口引用，确认本地正式配置仅含 16 个唯一 PM 且顺序正确；第二轮通过 5 项 Python 回归和 3 项模板网络重试回归，覆盖旧 Demo 字段／重复姓名拒绝、首次抓取失败不替换旧服务、网络失败后恢复和 HTTP 500 不重试；第三轮通过模板双脚本解析、敏感信息扫描、行尾与 `git diff --check`。使用正式 `start-dashboard.cmd` 在 8897 端口连续启动两次，旧服务等待新进程首次抓取期间 10 次探测全部 HTTP 200、连续失败 0 次；随后两次 `/api/data` 均返回 HTTP 200，离线文件同步更新。浏览器真实点击「AI 看板」后页面由「生成中」恢复为「重新生成」，数据时间更新至 15:57，控制台错误 0；当前已勾选 PM 按配置相对顺序展示，未命中固定成员隐藏。测试页面、服务和命令窗口均已关闭。
- 2026-07-22：规划导入产品代描述验证通过：模板两个 JavaScript 模块和既有网络重试回归均通过；正式双击入口在 8898 返回 HTTP 200 并加载最新模板，当天离线 HTML 于 16:32 更新。内嵌实时数据为 GT 46／N代基线 27／NOTE 28／CAMON 56／POVA 44，按新文案规则得到“2026年截止本周，规划导入GT的是46个，N代基线的是27个，NOTE的是28个，CAMON的是56个，POVA的是44个。”；测试服务和命令窗口已关闭。
- 2026-07-22：表格空值清理回归通过：11 项 Python 测试覆盖详细进展、开发达成、遗留问题和创新大赛的数据装配，前端测试确认空详细进展行仍保留 9 个单元格、空问题行仍保留 11 个单元格且不生成空状态徽章，空周 11 列占位结构继续存在；正文 `A/B`、`正文 / 内容` 和「暂无结论」均保持原样。Python 编译、模板双脚本解析、模板数据占位符、既有网络重试回归及 `git diff --check` 均通过；按用户安排未执行正式 Base 刷新或离线产物重生成。
- 2026-07-22：纠正表格空值口径并固化 Base 映射总原则：只有 `null`、空字符串和纯空白显示为空，Base 实际填写的 `/`、`／`、各类横线及「未填写／暂无／无」均保留；风险状态 `/` 显示中性徽章且不计风险，无法解析的非空日期保留原文并告警，人员字段的单独 `/` 不再被分隔逻辑吞掉。原则与稳妥边界已同步进入项目规范、README、生成脚本中央函数 `mapped_base_value()`、模板中央函数 `cellText()` 及 Python／JavaScript 测试；12 项 Python 回归、模板双脚本解析、单元格渲染、刷新重试、尾随空白、敏感信息和 `git diff --check` 均通过，并用测试守卫固定赛道数、PM 回退和最新日期块不被本次映射修复改变。以全新隐藏 `cmd.exe` 模拟双击正式脚本启动 8904 服务，首页 HTTP 200，首次抓取命中 14 个 PM 分组／90 个详细进展子任务，其中 1 条风险状态为 `/`；首页与当天离线 HTML 均加载最新映射且不含旧占位符黑名单，离线文件更新至 20:34，测试服务已停止。
- 2026-08-21：项目根目录空 `.git` 已由独立 Git 仓库初始化为 `main`；确认 `deployments/miaoda-app/` 仍为独立妙搭 App 仓库（`sprint/default`），并由项目 `.gitignore` 排除。首次提交 `766c341` 已推送至 GitHub `origin/main`，包含项目规范、源码、测试与文档；未触碰妙搭子仓库。
